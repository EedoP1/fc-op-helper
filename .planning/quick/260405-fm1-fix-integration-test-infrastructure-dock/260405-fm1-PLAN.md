---
phase: quick
plan: 260405-fm1
type: execute
wave: 1
depends_on: []
files_modified:
  - .dockerignore
  - src/server/db.py
  - scripts/run_integration_tests.sh
  - requirements.txt
  - pytest.ini
autonomous: true
requirements: []
must_haves:
  truths:
    - "Docker build excludes .venv, .git, .planning, __pycache__, tests, and other dev artifacts"
    - "API container survives Postgres starting slowly by retrying DB connection"
    - "Integration tests can be run with a single command"
    - "Individual tests timeout after a configured duration instead of hanging forever"
  artifacts:
    - path: ".dockerignore"
      provides: "Docker build context exclusions"
      contains: ".venv"
    - path: "src/server/db.py"
      provides: "DB connection retry logic"
      contains: "retry"
    - path: "scripts/run_integration_tests.sh"
      provides: "One-command integration test runner"
    - path: "pytest.ini"
      provides: "Default test timeout"
      contains: "timeout"
  key_links:
    - from: "src/server/db.py"
      to: "asyncpg"
      via: "retry loop in create_engine_and_tables"
      pattern: "retry|attempt"
---

<objective>
Fix four integration test infrastructure gaps: add .dockerignore to shrink build context from 340MB+, add DB connection retry to survive Postgres startup race, create a one-command test runner script, and add pytest-timeout to prevent hanging tests.

Purpose: Make integration tests reliable and fast to run, suitable for CI nightly builds.
Output: .dockerignore, updated db.py with retry, scripts/run_integration_tests.sh, pytest-timeout configured.
</objective>

<execution_context>
@C:\Users\maftu\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\maftu\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@docker-compose.yml
@docker-compose.test.yml
@Dockerfile
@src/server/db.py
@pytest.ini
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .dockerignore and DB connection retry</name>
  <files>.dockerignore, src/server/db.py</files>
  <action>
1. Create `.dockerignore` with these exclusions:
   ```
   .venv/
   .git/
   .planning/
   .claude/
   .agents/
   __pycache__/
   *.pyc
   tests/
   extension/
   node_modules/
   .pytest_cache/
   *.egg-info/
   .env
   .env.*
   ```
   Only src/, requirements.txt, dashboard.html, alembic/, alembic.ini, scripts/, and Dockerfile should be included in the build context.

2. In `src/server/db.py`, update `create_engine_and_tables()` to retry the initial DB connection. Wrap the `async with engine.begin()` block in a retry loop: 5 attempts, 2-second sleep between attempts, log each retry at WARNING level. On final failure, re-raise the exception. This handles the Docker race where Postgres accepts TCP connections but rejects queries with CannotConnectNowError.

   Use `import asyncio` and `import logging` (add logger = logging.getLogger(__name__) at module top). The retry loop should catch `Exception` broadly since different DB backends raise different connection errors. Log: `f"DB connection attempt {attempt}/{max_retries} failed: {e}. Retrying in {delay}s..."`. After all retries exhausted, log ERROR and re-raise.

   Do NOT add any new dependencies — use only asyncio.sleep for the delay.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/server/db.py').read()); print('db.py syntax OK')" && test -f .dockerignore && echo ".dockerignore exists" && python -c "content=open('.dockerignore').read(); assert '.venv' in content; assert '.git' in content; assert 'tests/' in content; print('dockerignore content OK')"</automated>
  </verify>
  <done>.dockerignore exists with all exclusions. create_engine_and_tables() retries 5 times with 2s backoff before giving up.</done>
</task>

<task type="auto">
  <name>Task 2: Add pytest-timeout and create test runner script</name>
  <files>requirements.txt, pytest.ini, scripts/run_integration_tests.sh</files>
  <action>
1. Add `pytest-timeout>=2.3.0` to `requirements.txt` (append after the existing pytest-asyncio line).

2. Update `pytest.ini` to add a default timeout. The file currently contains:
   ```
   [pytest]
   asyncio_mode = auto
   ```
   Add `timeout = 120` (2 minutes per test — generous enough for integration tests hitting real DB, strict enough to catch hangs). Result:
   ```
   [pytest]
   asyncio_mode = auto
   timeout = 120
   ```

3. Create `scripts/run_integration_tests.sh` — a self-contained script that:
   - Sets `set -euo pipefail` and prints each step
   - Ensures postgres-test is running: `docker compose up -d postgres-test` and waits for healthy
   - Builds and starts test containers: `docker compose -f docker-compose.yml -f docker-compose.test.yml -p op_seller_test up -d --build api scanner`
   - Waits for API health: polls `http://localhost:8001/health` up to 30 seconds (curl with --retry)
   - Runs pytest: `python -m pytest tests/integration/ -x -v --timeout=120` (uses the host Python, which connects to postgres-test on port 5433)
   - Tears down test containers on exit (trap): `docker compose -f docker-compose.yml -f docker-compose.test.yml -p op_seller_test down`
   - Does NOT tear down postgres-test (it persists across runs)
   - Make the script executable (chmod +x)

   The script header should include a usage comment explaining what it does and that it requires Docker and a local Python venv with requirements installed.
  </action>
  <verify>
    <automated>bash -n scripts/run_integration_tests.sh && echo "Script syntax OK" && python -c "content=open('requirements.txt').read(); assert 'pytest-timeout' in content; print('pytest-timeout in requirements')" && python -c "content=open('pytest.ini').read(); assert 'timeout' in content; print('timeout in pytest.ini')"</automated>
  </verify>
  <done>pytest-timeout added to requirements.txt. pytest.ini has 120s default timeout. scripts/run_integration_tests.sh exists, is executable, and handles the full lifecycle (start containers, wait for health, run tests, teardown).</done>
</task>

</tasks>

<verification>
- `docker build --no-cache -t op-seller-test .` completes in under 30 seconds (vs 60s+ before) and image size is smaller
- `python -c "from src.server.db import create_engine_and_tables"` imports without error
- `bash -n scripts/run_integration_tests.sh` passes syntax check
- `grep -c timeout pytest.ini` returns 1
</verification>

<success_criteria>
Docker builds are lean (no .venv/.git in context). API survives Postgres startup race via retry. Integration tests runnable with `bash scripts/run_integration_tests.sh`. Individual tests cannot hang indefinitely.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-fm1-fix-integration-test-infrastructure-dock/260405-fm1-SUMMARY.md`
</output>
