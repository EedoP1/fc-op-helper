---
phase: quick
plan: 260326-12g
subsystem: cli, server-api
tags: [cli, portfolio-api, display, startup]
dependency_graph:
  requires: []
  provides: [updated-cli-labels, op-sales-in-api, stale-score-purge]
  affects: [src/main.py, src/server/api/portfolio.py, src/server/main.py]
tech_stack:
  added: []
  patterns: [inline-import-to-avoid-circular, startup-purge]
key_files:
  created: []
  modified:
    - src/main.py
    - src/server/api/portfolio.py
    - src/server/main.py
decisions:
  - Inline import of `delete` and `PlayerScore` inside the async purge block to reduce circular-import risk in lifespan function
metrics:
  duration: 89s
  completed: 2026-03-25
  tasks: 2
  files: 3
---

# Phase quick Plan 260326-12g: Update CLI Labels for v2 Metrics and Purge Summary

CLI labels updated to reflect v2 scorer metrics (EP/hr, Sell%, OP Sales column), portfolio API now exposes op_sales/total_sales per player, and server startup purges stale v1 scores lacking expected_profit_per_hour.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add op_sales/total_sales to portfolio API and update CLI labels + CSV | 6f98559 | src/server/api/portfolio.py, src/main.py |
| 2 | Purge stale v1 scores at server startup | e857c19 | src/server/main.py |

## Changes Made

### Task 1: Portfolio API + CLI Labels

**src/server/api/portfolio.py:**
- Added `"op_sales": entry["op_sales"]` and `"total_sales": entry["total_sales"]` to the `data.append({...})` serialization loop

**src/main.py:**
- `run_portfolio()`: mapped dict now includes `"op_sold": item["op_sales"]` and `"op_total": item["total_sales"]`
- `display_results()`: renamed column "ExpProf" to "EP/hr", renamed "OP%" to "Sell%", added "OP Sales" column (sold/total format), updated `add_row()` to include `op_sold/op_total` value
- Summary panel: changed `"Expected profit:"` to `"Expected profit/hr:"`
- `export_csv()`: renamed header "Expected Profit" to "EP/hr", renamed "OP Ratio" to "Sell Rate", added "OP Sales" column; data rows now include `op_sold/op_total`

### Task 2: Startup Purge

**src/server/main.py:**
- Added purge block in `lifespan()` after `create_engine_and_tables()`, before scanner creation
- Executes `DELETE FROM player_scores WHERE expected_profit_per_hour IS NULL`
- Logs count of purged rows at INFO level if any were deleted

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None — all fields are wired to real data sources (op_sales/total_sales come from PlayerScore DB columns).

## Self-Check: PASSED

- src/main.py exists and imports ok
- src/server/api/portfolio.py exists and imports ok
- src/server/main.py exists and imports ok
- Commit 6f98559 exists (Task 1)
- Commit e857c19 exists (Task 2)
