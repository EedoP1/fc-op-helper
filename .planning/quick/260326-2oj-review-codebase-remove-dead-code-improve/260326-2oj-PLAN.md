---
phase: quick
plan: 260326-2oj
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/scorer.py
  - src/server/scorer_v2.py
  - src/server/listing_tracker.py
  - src/server/scanner.py
  - src/server/models_db.py
  - src/server/api/players.py
  - src/server/api/portfolio.py
  - src/futgg_client.py
  - tests/test_scorer.py
  - tests/test_scorer_v2.py
  - tests/test_scanner.py
  - tests/test_optimizer.py
  - tests/test_api.py
  - tests/test_integration.py
  - tests/test_portfolio.py
  - tests/test_cli.py
  - tests/test_db.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "No production code references dead v1 score_player function"
    - "MARGINS constant lives in config.py, the single source of truth for all configuration"
    - "scorer.py is deleted entirely"
    - "No .bak files remain in the repo"
    - "scorer_version field removed from API responses (never populated)"
    - "All existing tests pass after cleanup"
  artifacts:
    - path: "src/config.py"
      provides: "MARGINS constant moved here from scorer.py"
      contains: "MARGINS"
    - path: "src/server/scorer_v2.py"
      provides: "v2 scorer importing MARGINS from config"
    - path: "src/server/listing_tracker.py"
      provides: "listing tracker importing MARGINS from config"
  key_links:
    - from: "src/server/scorer_v2.py"
      to: "src/config.py"
      via: "from src.config import ... MARGINS"
      pattern: "from src\\.config import.*MARGINS"
    - from: "src/server/listing_tracker.py"
      to: "src/config.py"
      via: "from src.config import ... MARGINS"
      pattern: "from src\\.config import.*MARGINS"
---

<objective>
Remove dead code left behind by the v1-to-v2 scorer migration and several quick-task iterations.
Clean up vestigial fields that are never meaningfully populated.

Purpose: Reduce cognitive load for future development by eliminating code that appears
functional but is actually unreachable. The codebase went through rapid iteration
(9 quick tasks in one day) and accumulated dead paths that could mislead future work.

Output: Cleaner codebase with no dead code, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/config.py
@src/scorer.py
@src/server/scorer_v2.py
@src/server/listing_tracker.py
@src/server/scanner.py
@src/server/models_db.py
@src/server/api/players.py
@src/server/api/portfolio.py
@src/futgg_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead v1 scorer and relocate MARGINS constant</name>
  <files>
    src/scorer.py
    src/config.py
    src/server/scorer_v2.py
    src/server/listing_tracker.py
    tests/test_scorer.py
  </files>
  <action>
1. Move the MARGINS constant from src/scorer.py to src/config.py. Place it after the
   existing listing-tracking constants, with a comment:
   ```python
   # OP sell margin tiers (highest first)
   MARGINS = [40, 35, 30, 25, 20, 15, 10, 8, 5, 3]
   ```

2. Update imports in src/server/scorer_v2.py:
   - Change `from src.scorer import MARGINS` to include MARGINS in the existing
     `from src.config import (...)` block.

3. Update imports in src/server/listing_tracker.py:
   - Remove `from src.scorer import MARGINS` entirely.
   - Add MARGINS to the existing `from src.config import MIN_OP_OBSERVATIONS` line,
     making it `from src.config import MIN_OP_OBSERVATIONS, MARGINS`.
   - Also remove the `# noqa: F401` on the MIN_OP_OBSERVATIONS import since
     MIN_OP_OBSERVATIONS IS used in this module (it's passed through from config
     but the noqa comment claims it's an "exported constant" which is misleading --
     actually check: grep for MIN_OP_OBSERVATIONS usage in listing_tracker.py.
     If it truly is not used in this file, keep the noqa but remove the misleading
     comment. Actually: MIN_OP_OBSERVATIONS is NOT used in listing_tracker.py at all --
     it's only used in scorer_v2.py. Remove the import entirely from listing_tracker.py.

4. Delete src/scorer.py entirely. The score_player() function, its helper
   _get_price_at_time(), and its constants (MIN_OP_SALES, MIN_TOTAL_SALES,
   MIN_LIVE_LISTINGS, MIN_SALES_PER_HOUR) are all dead code -- score_player()
   is never called from any production code path. Only MARGINS was still needed
   and has been moved to config.py.

5. Delete tests/test_scorer.py -- it tests the deleted score_player() function.

6. Delete the .bak files:
   - src/server/scorer_v2.py.bak
   - tests/test_scorer_v2.py.bak
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -c "from src.config import MARGINS; print('MARGINS ok:', MARGINS)" && python -c "from src.server.scorer_v2 import score_player_v2; print('scorer_v2 import ok')" && python -c "from src.server.listing_tracker import record_listings; print('listing_tracker import ok')" && python -c "import importlib; assert importlib.util.find_spec('src.scorer') is None; print('scorer.py deleted')"</automated>
  </verify>
  <done>
    - src/scorer.py deleted
    - tests/test_scorer.py deleted
    - .bak files deleted
    - MARGINS lives in src/config.py
    - scorer_v2.py and listing_tracker.py import MARGINS from config
    - All imports resolve without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead methods, vestigial fields from API responses, and clean up scanner</name>
  <files>
    src/futgg_client.py
    src/server/api/players.py
    src/server/api/portfolio.py
    src/server/scanner.py
    tests/test_api.py
    tests/test_integration.py
    tests/test_portfolio.py
    tests/test_scanner.py
    tests/test_cli.py
    tests/test_db.py
    tests/test_optimizer.py
  </files>
  <action>
1. Delete FutGGClient.log_live_auction_fields() static method from src/futgg_client.py
   (lines 265-276). It is never called from any production code -- it was a diagnostic
   method from Phase 04 plan 01 that was superseded by the fingerprint strategy in plan 02.

2. Remove `scorer_version` from API responses:
   - In src/server/api/players.py get_top_players(): remove the "scorer_version" key
     from the player dict (line ~98).
   - In src/server/api/players.py get_player(): remove "scorer_version" from
     current_score dict (line ~210).
   - In src/server/api/players.py get_player(): remove "scorer_version" from
     score_history entries if present.

3. Remove `scan_tier` from API responses where it adds no value:
   - In src/server/api/players.py get_top_players(): remove "scan_tier" key (line ~94).
   - In src/server/api/players.py get_player(): remove "scan_tier" key from response.
   - In src/server/api/portfolio.py: remove "scan_tier" from response dicts (line ~143).
   NOTE: Keep scan_tier in the DB model (PlayerRecord) -- it may be useful for future
   features. Just stop exposing it in API responses since it's always empty string now.

4. Remove `sales_per_hour` from scanner writes where it's always hardcoded to 0.0:
   - In src/server/scanner.py scan_player(): the v2 branch sets sales_per_hour=0.0
     on both the PlayerScore and the PlayerRecord update. The PlayerScore model requires
     this field, so keep setting it to 0.0 there. But remove the redundant
     `record.sales_per_hour = 0.0` line (line ~386) in the PlayerRecord update since
     it's already 0.0 from bootstrap and never changes.

5. Update all affected tests to remove assertions on deleted fields:
   - test_api.py: Remove scan_tier and scorer_version assertions from response checks.
   - test_portfolio.py: Remove scan_tier from response assertions.
   - test_cli.py: Remove scan_tier from mock response data if present.
   - test_integration.py: Remove scorer_version assertions.
   - Do NOT change test_db.py -- the DB model still has these columns; tests validating
     the schema should remain.

6. Run the full test suite to verify nothing breaks.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - log_live_auction_fields() deleted from FutGGClient
    - scorer_version removed from all API responses
    - scan_tier removed from all API responses
    - Redundant sales_per_hour=0.0 write removed from scanner PlayerRecord update
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passes with 0 failures
- `grep -r "from src.scorer import" src/` returns NO matches (only config imports MARGINS now)
- `grep -r "log_live_auction_fields" src/` returns NO matches
- `grep -r "scorer_version" src/server/api/` returns NO matches
- No .bak files exist in the repo
</verification>

<success_criteria>
- All dead code identified in the review is removed
- MARGINS constant relocated to config.py (single source of truth)
- All production imports resolve correctly
- Full test suite passes
- No functional behavior changes -- only dead code removal and cleanup
</success_criteria>

<output>
After completion, create `.planning/quick/260326-2oj-review-codebase-remove-dead-code-improve/260326-2oj-SUMMARY.md`
</output>
