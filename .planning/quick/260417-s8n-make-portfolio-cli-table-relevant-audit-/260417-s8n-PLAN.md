---
phase: quick-260417-s8n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/api/portfolio_read.py
  - src/main.py
autonomous: false
requirements:
  - Q-01-expose-missing-portfolio-fields
  - Q-02-rework-cli-table-columns
  - Q-03-fix-ep-per-hour-bugs

must_haves:
  truths:
    - "CLI EP/hr column displays expected_profit_per_hour (not per-flip expected_profit)"
    - "CLI summary panel 'Expected profit/hr' sums expected_profit_per_hour across selected players"
    - "CLI table has a Sell column showing the listing price"
    - "CLI table has a Profit column showing net_profit per flip (post EA tax)"
    - "CLI table has a Sales/hr column showing liquidity"
    - "CLI table no longer shows the Efficiency column"
    - "CLI column previously labeled 'Sell%' is relabeled 'Win%' (op_ratio)"
    - "Rows from stale scores are visually marked (dim style or * indicator)"
    - "CSV export contains the same columns as the CLI table"
    - "GET /api/v1/portfolio response includes sell_price, net_profit, sales_per_hour, card_type for each player"
  artifacts:
    - path: "src/server/api/portfolio_read.py"
      provides: "GET /portfolio response now exposes sell_price, net_profit, sales_per_hour, card_type"
      contains: "sell_price"
    - path: "src/main.py"
      provides: "Reworked portfolio table: identifier + economics + liquidity columns; correct EP/hr wiring; stale row indicator"
      contains: "expected_profit_per_hour"
  key_links:
    - from: "src/server/api/portfolio_read.py (GET /portfolio response dict)"
      to: "src/main.py run_portfolio mapped dict"
      via: "JSON keys sell_price, net_profit, sales_per_hour, card_type"
      pattern: "sell_price|net_profit|sales_per_hour|card_type"
    - from: "src/main.py run_portfolio mapped dict"
      to: "src/main.py display_results table + summary"
      via: "expected_profit_per_hour field read instead of expected_profit for EP/hr"
      pattern: "expected_profit_per_hour"
---

<objective>
Fix the portfolio CLI so every column earns its place for an OP seller. Repair two wrong-field bugs (EP/hr column and summary both currently show per-flip profit, not per-hour), surface data the server already computes but never sent (sell_price, net_profit, sales_per_hour, card_type), drop the meaningless Efficiency column, and dim rows with stale scores.

Purpose: The CLI is supposed to answer "what do I buy, what do I list at, what do I make per flip, and how fast does it earn per hour." It currently answers the first question, partially answers the second, and mislabels the fourth.
Output: Additive `/api/v1/portfolio` response fields + reworked CLI table + CSV export matching the new columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/main.py
@src/server/api/portfolio_read.py
@src/server/api/portfolio_query.py

<interfaces>
<!-- Server-side: _build_scored_entry (src/server/api/portfolio_query.py:19) already builds every field we need per player: -->
<!--   buy_price, sell_price, net_profit, margin_pct, op_sales, total_sales, op_ratio, -->
<!--   expected_profit, efficiency, sales_per_hour, card_type, last_scanned_at, -->
<!--   expected_profit_per_hour, futgg_url, ea_id, name, rating, position -->
<!-- GET /portfolio (portfolio_read.py:83) currently serializes only a subset; sell_price, net_profit, sales_per_hour, card_type are NOT surfaced. -->
<!-- POST /portfolio/generate (same file, line 166) already surfaces sell_price; use its shape as reference when extending GET. -->
<!-- CLI run_portfolio (main.py:61) maps API JSON -> internal dict consumed by display_results (main.py:161) and export_csv (main.py:212). -->
<!-- The CLI currently reads item["expected_profit"] and displays it under the "EP/hr" header — this is the primary bug. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose sell_price, net_profit, sales_per_hour, card_type in GET /portfolio response</name>
  <files>src/server/api/portfolio_read.py</files>
  <action>
In `get_portfolio` (the GET /portfolio handler starting at line 19), extend the per-player dict appended to `data` (lines 83-101) to include four additional keys, sourced from the already-built `entry` dict:

- `"sell_price": entry["sell_price"]`
- `"net_profit": entry["net_profit"]`
- `"sales_per_hour": round(entry["sales_per_hour"], 1) if entry.get("sales_per_hour") is not None else None`
- `"card_type": entry.get("card_type")`

Match the style already used by POST /portfolio/generate (lines 166-185 in this same file), which already exposes `sell_price`. Do NOT touch `/portfolio/generate`, `/portfolio/swap-preview`, `/portfolio/confirmed`, `/portfolio/actions-needed`, or `/portfolio/rebalance` — scope is the GET /portfolio response only.

This is a purely additive change: do not rename, reorder, or remove any existing keys. No existing consumer (extension service worker, tests) depends on these keys being absent.

No new imports needed (`entry` already has all fields from `_build_scored_entry`).
  </action>
  <verify>
    <automated>cd /c/Users/maftu/Projects/op-seller &amp;&amp; python -c "import ast; tree = ast.parse(open('src/server/api/portfolio_read.py').read()); src = open('src/server/api/portfolio_read.py').read(); assert 'sell_price' in src.split('def generate_portfolio')[0].split('def get_portfolio')[1], 'sell_price missing from GET /portfolio'; assert 'net_profit' in src.split('def generate_portfolio')[0].split('def get_portfolio')[1], 'net_profit missing from GET /portfolio'; assert 'sales_per_hour' in src.split('def generate_portfolio')[0].split('def get_portfolio')[1], 'sales_per_hour missing from GET /portfolio'; assert 'card_type' in src.split('def generate_portfolio')[0].split('def get_portfolio')[1], 'card_type missing from GET /portfolio'; print('OK: all 4 fields present in GET /portfolio')"</automated>
  </verify>
  <done>GET /portfolio response dict now contains `sell_price`, `net_profit`, `sales_per_hour`, and `card_type`. Python file imports and parses cleanly. No other endpoint in the file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Rework portfolio CLI table and CSV export; fix EP/hr wiring</name>
  <files>src/main.py</files>
  <action>
Rework `run_portfolio` (line 35), `display_results` (line 161), and `export_csv` (line 212) to show columns that match an OP seller's mental model. All source data is in the API response after Task 1.

**1. `run_portfolio` — extend the mapped dict (line 61-74).**

Read the new fields from the API response. The mapped dict should include (keep existing keys, add new ones, do not drop `expected_profit` — it's still useful as per-flip "Profit" once renamed downstream):

```python
mapped = [
    {
        "player_name": item["name"],
        "rating": item["rating"],
        "position": item["position"],
        "card_type": item.get("card_type"),
        "buy_price": item["price"],
        "sell_price": item["sell_price"],
        "net_profit": item["net_profit"],
        "margin_pct": item["margin_pct"],
        "op_sold": item["op_sales"],
        "op_total": item["total_sales"],
        "op_ratio": item["op_ratio"],
        "expected_profit": item["expected_profit"],  # per-flip
        "expected_profit_per_hour": item.get("expected_profit_per_hour"),  # ranking metric
        "sales_per_hour": item.get("sales_per_hour"),
        "is_stale": item.get("is_stale", False),
    }
    for item in data["data"]
]
```

**2. `display_results` — fix summary math and rebuild the table.**

Fix Bug 2 in the summary (line 173): sum `expected_profit_per_hour` instead of `expected_profit`. Handle None defensively:

```python
total_eph = sum((s.get("expected_profit_per_hour") or 0) for s in selected)
```

Use `total_eph` in the "Expected profit/hr" panel line (currently line 180) — the label is already correct, only the source was wrong.

Replace the table column definitions (lines 184-194) with:

| Column | Header | Source | Format |
|--------|--------|--------|--------|
| 1 | # | index+1 | str |
| 2 | Player | `player_name` (truncated to 20) | str |
| 3 | OVR | `rating` | str |
| 4 | Pos | `position` | str |
| 5 | Buy | `buy_price` | `{v:,}` |
| 6 | Sell | `sell_price` | `{v:,}` |
| 7 | Profit | `net_profit` | `{v:,}` |
| 8 | Margin | `margin_pct` | `{v}%` |
| 9 | EP/hr | `expected_profit_per_hour` | `{v:,.0f}` (show `-` if None) |
| 10 | Win% | `op_ratio` | `{v:.0%}` |
| 11 | OP Sales | `op_sold`/`op_total` | `{a}/{b}` |
| 12 | Sales/hr | `sales_per_hour` | `{v:.1f}` (show `-` if None) |

- Drop the old `Efficiency` column entirely.
- Rename header `Sell%` -> `Win%` (semantics: "share of sales that cleared OP margin").
- Keep the cyan style on EP/hr (it's the ranking metric).
- Right-justify all numeric columns; center OVR and Pos (already the pattern at line 187-188).

For the stale indicator: when `s["is_stale"]` is True, prepend `*` to the player name cell AND apply a dim style to that row. Simplest approach: pass `style="dim"` as a kwarg on `table.add_row(...)` when stale, and use `Text` objects if needed — but since `add_row` accepts a `style=` parameter in Rich, use that. Example:

```python
row_style = "dim" if s.get("is_stale") else None
name_cell = f"*{s['player_name'][:19]}" if s.get("is_stale") else s["player_name"][:20]
table.add_row(
    str(i + 1),
    name_cell,
    str(s["rating"]),
    s["position"],
    f"{s['buy_price']:,}",
    f"{s['sell_price']:,}",
    f"{s['net_profit']:,}",
    f"{s['margin_pct']}%",
    f"{s['expected_profit_per_hour']:,.0f}" if s.get("expected_profit_per_hour") is not None else "-",
    f"{s['op_ratio']:.0%}",
    f"{s['op_sold']}/{s['op_total']}",
    f"{s['sales_per_hour']:.1f}" if s.get("sales_per_hour") is not None else "-",
    style=row_style,
)
```

**3. `export_csv` — mirror the new column set.**

Update header row and row writer (lines 225-235) to: `Rank, Player, Rating, Position, Buy, Sell, Profit, Margin, EP/hr, Win%, OP Sales, Sales/hr, Stale`.

- `Stale` column is a bool (`1` if `is_stale` else `0`) — easier to filter in a spreadsheet than a leading `*`.
- Format numbers the same way they appear in the table; `expected_profit_per_hour` and `sales_per_hour` fall back to empty string if None.

**4. Do NOT touch `run_player_detail` (line 86) or the `main()` CLI entry (line 247).** The player-detail path has its own table and is out of scope for this task.

**5. Do NOT add tests.** Per CLAUDE.md conventions this is a display layer; existing server endpoints are unit-tested elsewhere and the additive GET /portfolio change is trivially compatible.
  </action>
  <verify>
    <automated>cd /c/Users/maftu/Projects/op-seller &amp;&amp; python -c "
import ast, re
src = open('src/main.py').read()
ast.parse(src)  # syntax check

# Bug 1 fix: EP/hr column must read expected_profit_per_hour
disp = src.split('def display_results')[1].split('def export_csv')[0]
assert 'expected_profit_per_hour' in disp, 'display_results still not using expected_profit_per_hour'

# Bug 2 fix: summary must sum expected_profit_per_hour, not expected_profit-only
assert re.search(r'sum\(.*expected_profit_per_hour', disp), 'summary still sums wrong field'

# New columns present
for col in ['Sell', 'Profit', 'Sales/hr', 'Win%']:
    assert col in disp, f'column {col!r} missing from display_results'

# Efficiency column removed from the table
# (string Efficiency may still appear in player-detail section above; only enforce for display_results block)
assert 'Efficiency' not in disp, 'Efficiency column still present in display_results'

# CSV export updated
csv_blk = src.split('def export_csv')[1]
for col in ['Sell', 'Profit', 'Sales/hr', 'Win%']:
    assert col in csv_blk, f'column {col!r} missing from export_csv'
assert 'Efficiency' not in csv_blk, 'Efficiency column still present in export_csv'

# run_portfolio mapped dict must read the new API fields
rp = src.split('async def run_portfolio')[1].split('async def run_player_detail')[0]
for fld in ['sell_price', 'net_profit', 'sales_per_hour', 'is_stale']:
    assert fld in rp, f'run_portfolio mapped dict missing {fld!r}'
print('OK: all CLI display checks pass')
"</automated>
  </verify>
  <done>CLI table has columns: #, Player, OVR, Pos, Buy, Sell, Profit, Margin, EP/hr, Win%, OP Sales, Sales/hr. Efficiency column is gone. Summary panel's "Expected profit/hr" sums `expected_profit_per_hour`. Stale rows are dim and marked with `*`. CSV export mirrors the new columns and adds a Stale boolean column. `python -m src.main --budget 1000000` runs without error (checkpoint step below confirms).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of reworked portfolio CLI output</name>
  <files>src/main.py (runtime verification of CLI output)</files>
  <action>Pause for the user to run the CLI and visually confirm the new columns, stale-row rendering, and CSV output. See &lt;how-to-verify&gt; for the exact steps.</action>
  <verify>User runs the steps in &lt;how-to-verify&gt; below and replies "approved".</verify>
  <done>User confirms the new table and CSV match expectations, EP/hr number is sourced from expected_profit_per_hour, and stale rows render dimmed when present.</done>
  <what-built>Portfolio CLI now queries the extended `/api/v1/portfolio` response and renders columns aligned with an OP seller's workflow (Buy, Sell, Profit, Margin, EP/hr, Win%, OP Sales, Sales/hr). The per-hour bug in EP/hr and summary is fixed. Stale rows are visually marked.</what-built>
  <how-to-verify>
Assumes backend is already running (uvicorn src.server.main:app). If not: start it first.

1. From the repo root run:
   ```
   python -m src.main --budget 1000000
   ```
2. Confirm the summary panel shows "Expected profit/hr: {N}" where the number is materially different from (typically much smaller than) the old per-flip-sum value — because it's now coins-per-hour, not coins-per-flip-summed. A rough sanity check: if the portfolio has ~100 players, this number should be in the range of tens-of-thousands to low-hundreds-of-thousands coins/hr, not millions.
3. Confirm the table has these columns in this order: `# | Player | OVR | Pos | Buy | Sell | Profit | Margin | EP/hr | Win% | OP Sales | Sales/hr`. No `Efficiency` column. No `Sell%` header (it's now `Win%`).
4. Spot-check a few rows: `Sell > Buy`, `Profit ≈ (Sell × 0.95) − Buy` (5% EA tax), `Margin%` is consistent with buy/sell, `Sales/hr` is a small decimal (typical range: 5-60).
5. Confirm that if any rows are stale (`is_stale=true`), they appear dimmed and the player name starts with `*`. If the scanner is healthy this may not trigger — that's fine, the absence of stale rows is acceptable.
6. Open the emitted `op_sell_list_*.csv` and confirm the headers are `Rank, Player, Rating, Position, Buy, Sell, Profit, Margin, EP/hr, Win%, OP Sales, Sales/hr, Stale`.
  </how-to-verify>
  <resume-signal>Type "approved" to commit, or describe anything off.</resume-signal>
</task>

</tasks>

<verification>
- `python -m src.main --budget 1000000` completes without error and renders the new 12-column table.
- The emitted CSV has 13 columns matching the new scheme.
- Summary "Expected profit/hr" value is sourced from the sum of `expected_profit_per_hour` (not per-flip `expected_profit`).
- GET /api/v1/portfolio JSON response (`curl "http://localhost:8000/api/v1/portfolio?budget=1000000"`) returns each entry with keys including `sell_price`, `net_profit`, `sales_per_hour`, `card_type` in addition to existing keys.
- No regressions: `run_player_detail` (the `--player` flag path) still works because it was untouched.
</verification>

<success_criteria>
- GET /portfolio response includes sell_price, net_profit, sales_per_hour, card_type per entry (additive).
- CLI table renders Buy, Sell, Profit, Margin, EP/hr, Win%, OP Sales, Sales/hr — Efficiency and Sell% headers gone.
- EP/hr column and summary panel both reflect expected_profit_per_hour.
- Stale rows are dim and flagged with `*`.
- CSV export mirrors the new columns plus a Stale boolean.
- Human checkpoint passed.
</success_criteria>

<output>
After completion, create `.planning/quick/260417-s8n-make-portfolio-cli-table-relevant-audit-/260417-s8n-SUMMARY.md`.
</output>
