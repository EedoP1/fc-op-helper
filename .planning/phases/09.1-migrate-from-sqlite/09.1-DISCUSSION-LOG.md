# Phase 09.1: Migrate from SQLite - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-28
**Phase:** 09.1-migrate-from-sqlite
**Areas discussed:** Hosting & connection setup, Data migration strategy, Dialect swap scope, Test & rollback plan

---

## Hosting & Connection Setup

### Where should PostgreSQL run?

| Option | Description | Selected |
|--------|-------------|----------|
| Local Docker | docker-compose with Postgres container. Zero cost, full control, easy to recreate. | ✓ |
| Cloud-managed (Supabase/Neon) | Hosted Postgres with free tier. Adds external dependency. | |
| Bare-metal install | Install Postgres directly on Windows. Simplest but harder to reproduce. | |

**User's choice:** Local Docker
**Notes:** None

### Connection string management

| Option | Description | Selected |
|--------|-------------|----------|
| Environment variable | DATABASE_URL env var with .env file for local dev. | ✓ |
| .env file only | No env var fallback in code. | |
| Config file | Store in config.json or similar. | |

**User's choice:** Environment variable
**Notes:** None

### Engine consolidation

| Option | Description | Selected |
|--------|-------------|----------|
| Single engine + pool | One engine with asyncpg connection pool. Postgres MVCC eliminates 3-engine need. | ✓ |
| Keep read/write separation | Maintain separate engines for future read replicas. | |

**User's choice:** Single engine + pool
**Notes:** None

---

## Data Migration Strategy

### Migration tool

| Option | Description | Selected |
|--------|-------------|----------|
| pgloader one-shot | Reads SQLite directly, bulk-loads to Postgres. ~5-10 min for 7GB. | ✓ |
| SQLAlchemy script | Custom Python script. More control but slower. | |
| CSV export/import | Low-level dump/transform/load. Manual but transparent. | |

**User's choice:** pgloader one-shot
**Notes:** None

### SQLite file retention

| Option | Description | Selected |
|--------|-------------|----------|
| Keep as backup | Don't delete .db file. Enables quick rollback. | ✓ |
| Clean break | Delete SQLite file and aiosqlite dependency. | |
| Dual-write period | Run both databases simultaneously. | |

**User's choice:** Keep as backup
**Notes:** None

### Downtime tolerance

| Option | Description | Selected |
|--------|-------------|----------|
| Brief downtime fine | Stop scanner, migrate, restart. 5-10 min gap. | ✓ |
| Zero downtime | Dual-write or incremental sync. | |

**User's choice:** Brief downtime fine
**Notes:** None

---

## Dialect Swap Scope

### health_check.py

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite to SQLAlchemy | Port to async SQLAlchemy. Eliminates last raw sqlite3 dependency. | ✓ |
| Rewrite to raw psycopg2 | Swap sqlite3 for psycopg2. Keeps standalone pattern. | |
| Remove health_check.py | Module references FUTBIN data (already removed). Maybe not useful. | |

**User's choice:** Rewrite to SQLAlchemy
**Notes:** None

### Schema cleanup during migration

| Option | Description | Selected |
|--------|-------------|----------|
| Clean up during migration | Drop futbin_id, set scan_tier defaults, backfill scorer_version. | ✓ |
| Migrate as-is | Copy verbatim, clean up later. | |
| You decide | Claude picks based on complexity. | |

**User's choice:** Clean up during migration
**Notes:** None

---

## Test & Rollback Plan

### Integration test DB

| Option | Description | Selected |
|--------|-------------|----------|
| Docker test container | Disposable Postgres container per test run. Fresh DB each time. | ✓ |
| Shared local Postgres | Same instance, separate test database. | |
| SQLite for tests only | Keep SQLite for tests, Postgres for prod. Risks dialect divergence. | |

**User's choice:** Docker test container
**Notes:** None

### Verification bar

| Option | Description | Selected |
|--------|-------------|----------|
| All Phase 9 tests pass | 40+ integration tests against Postgres. | |
| Phase 9 tests + manual spot-check | Tests + manual key query verification. | |
| You decide | Claude picks verification level. | |

**User's choice:** Other (free text)
**Notes:** "I want at the end all Phase 9 tests to run, the problem is they never worked yet probably because of server side issues, the migration is part of fixing the server"

### Volatility cache removal

| Option | Description | Selected |
|--------|-------------|----------|
| Remove cache, fix query | Postgres makes query fast enough. Remove volatility_cache.py. Simpler code. | ✓ |
| Keep cache as optimization | Even if Postgres is fast, cache reduces DB load. | |
| You decide | Evaluate during execution based on actual performance. | |

**User's choice:** Remove cache, fix query
**Notes:** None

---

## Claude's Discretion

- pgloader configuration details
- asyncpg connection pool sizing
- docker-compose specifics
- Post-migration SQL cleanup script
- Alembic vs create_all for schema management

## Deferred Ideas

None — discussion stayed within phase scope
