---
phase: 09.1-migrate-from-sqlite
plan: 02
subsystem: database
tags: [postgresql, sqlalchemy, sqlite, migration, health-check]

# Dependency graph
requires:
  - phase: 09.1-migrate-from-sqlite plan 01
    provides: PostgreSQL engine, single async_sessionmaker, asyncpg connection pool
provides:
  - PostgreSQL upserts (pg_insert) for PlayerRecord in scanner.py
  - PostgreSQL upserts (pg_insert) for ListingObservation and DailyListingSummary in listing_tracker.py
  - Corrected DailyListingSummary upsert conflict target using UniqueConstraint
  - health_check.py fully migrated from raw sqlite3 to async SQLAlchemy
  - volatility_cache.py confirmed absent with no wiring in main.py
affects: [testing, scanner, listing-tracker, health-check]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "pg_insert(Model).values(...).on_conflict_do_update(constraint=name, set_=...) for named-constraint upserts"
    - "async def with AsyncSession for all DB-touching health-check helpers"
    - "asyncio.run(_async_main()) wrapper pattern for Click commands needing async DB access"

key-files:
  created: []
  modified:
    - src/server/scanner.py
    - src/server/listing_tracker.py
    - src/server/models_db.py
    - src/health_check.py

key-decisions:
  - "DailyListingSummary upsert uses constraint= not index_elements= because conflict target is a UniqueConstraint, not a plain index"
  - "health_check.py futbin_id caching removed — UPDATE players SET futbin_id is a no-op since column is being dropped (D-10)"
  - "volatility_cache.py was never created and main.py had no references — D-13 was already satisfied"

patterns-established:
  - "Named UniqueConstraint pattern: add UniqueConstraint(cols, name=) in __table_args__ then reference by constraint= in pg_insert upsert"

requirements-completed: [D-07, D-08, D-10, D-13]

# Metrics
duration: 8min
completed: 2026-03-28
---

# Phase 09.1 Plan 02: Replace SQLite Dialect Code and Migrate health_check.py Summary

**All sqlite_insert upserts replaced with pg_insert, DailyListingSummary upsert bug fixed with composite UniqueConstraint, and health_check.py rewritten from raw sqlite3 to async SQLAlchemy**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-28T14:55:00Z
- **Completed:** 2026-03-28T14:58:31Z
- **Tasks:** 3 (Tasks 1 and 2 committed; Task 3 confirmed no-op)
- **Files modified:** 4

## Accomplishments
- Replaced all 4 `sqlite_insert` calls with `pg_insert` across scanner.py (2 upserts) and listing_tracker.py (2 upserts)
- Fixed pre-existing bug in DailyListingSummary upsert: `index_elements=["id"]` was wrong — the conflict target is a composite key, not the PK. Now uses `constraint="uq_daily_summary_ea_id_date_margin"` backed by a new `UniqueConstraint` in models_db.py
- Rewrote health_check.py removing all `sqlite3` / `DB_PATH` / `_ensure_schema` code; replaced with async SQLAlchemy sessions, ORM queries, and `asyncio.run()` CLI entry point
- Confirmed `volatility_cache.py` does not exist and `main.py` has zero references — D-13 already satisfied, no action needed

## Task Commits

1. **Task 1: Replace sqlite_insert with pg_insert, fix DailyListingSummary upsert bug** - `3a6b8fb` (feat)
2. **Task 2: Rewrite health_check.py from raw sqlite3 to async SQLAlchemy** - `3b6beab` (feat)
3. **Task 3: Remove volatility_cache.py** - no commit required (file was never created; main.py was already clean)

## Files Created/Modified
- `src/server/scanner.py` - Replaced `sqlite_insert` with `pg_insert` at 2 PlayerRecord upsert sites
- `src/server/listing_tracker.py` - Replaced `sqlite_insert` with `pg_insert` at 3 sites; fixed DailyListingSummary conflict target
- `src/server/models_db.py` - Added `UniqueConstraint("ea_id", "date", "margin_pct", name="uq_daily_summary_ea_id_date_margin")` to DailyListingSummary
- `src/health_check.py` - Full rewrite: removed sqlite3/DB_PATH, added async SQLAlchemy ORM queries, asyncio.run() entry point

## Decisions Made
- `constraint=` instead of `index_elements=` in DailyListingSummary upsert: PostgreSQL requires naming the unique constraint explicitly when the conflict target is a multi-column UniqueConstraint (not a plain index). Added matching `UniqueConstraint` to the ORM model.
- `futbin_id` caching removed from health_check: the `futbin_id` column is being dropped in D-10; no sense caching to a column that will be removed.
- Task 3 is a no-op: `volatility_cache.py` was never created, and `main.py` had no references to it. D-13 was already satisfied without action.

## Deviations from Plan

None - plan executed exactly as written. Task 3 was satisfied by the current state of the codebase (file never existed, wiring never added).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All SQLite dialect code eliminated from scanner.py, listing_tracker.py, health_check.py
- DailyListingSummary has correct UniqueConstraint for Postgres upserts to work
- Ready for Plan 03: migration script, Alembic setup, and final validation

---
*Phase: 09.1-migrate-from-sqlite*
*Completed: 2026-03-28*
