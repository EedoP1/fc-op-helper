---
phase: 09.1-migrate-from-sqlite
plan: 01
subsystem: database
tags: [postgres, asyncpg, docker, sqlalchemy, connection-pool]

# Dependency graph
requires: []
provides:
  - docker-compose.yml with postgres:17 container definition
  - .env.example with postgresql+asyncpg DATABASE_URL template
  - Single asyncpg engine with pool_size=10, max_overflow=20, pool_pre_ping=True in db.py
  - Removed 3-engine SQLite split (create_read_engine, create_api_write_engine deleted)
  - Removed all SQLite PRAGMA event listeners from db.py
  - Consolidated main.py lifespan to one engine — read_session_factory = session_factory
affects: [09.1-02, 09.1-03, tests/integration]

# Tech tracking
tech-stack:
  added: [asyncpg>=0.30.0, python-dotenv>=1.0.0, testcontainers[postgres]>=4.0.0]
  patterns: [single-engine asyncpg pool for all read/write (MVCC handles concurrency), pool_pre_ping for connection health]

key-files:
  created:
    - docker-compose.yml
    - .env.example
  modified:
    - src/config.py
    - requirements.txt
    - src/server/db.py
    - src/server/main.py

key-decisions:
  - "Single asyncpg connection pool replaces 3-engine SQLite split — Postgres MVCC handles concurrent scanner + API access without separate engines"
  - "app.state.read_session_factory = session_factory — same pool, attribute name preserved so API endpoints need no changes"
  - "aiosqlite kept in requirements.txt for rollback safety per D-06"

patterns-established:
  - "Single engine pattern: one create_engine() call, one session_factory, assigned to both session_factory and read_session_factory app.state attrs"

requirements-completed: [D-01, D-02, D-03, D-09]

# Metrics
duration: 2min
completed: 2026-03-28
---

# Phase 09.1 Plan 01: PostgreSQL Infrastructure and Single-Engine DB Layer Summary

**Postgres 17 Docker container + asyncpg single-engine db.py replacing 3-engine SQLite split with PRAGMA removal**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-28T14:50:06Z
- **Completed:** 2026-03-28T14:52:36Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created docker-compose.yml with postgres:17 container, healthcheck, and named volume
- Rewrote db.py from 3-engine SQLite architecture to single asyncpg pool (pool_size=10, max_overflow=20, pool_pre_ping=True)
- Removed all SQLite PRAGMA event listeners (WAL, synchronous, foreign_keys, query_only)
- Consolidated main.py lifespan to one engine — no more read_engine creation/disposal
- Added asyncpg, python-dotenv, testcontainers[postgres] to requirements.txt

## Task Commits

Each task was committed atomically:

1. **Task 1: Docker + deps + config** - `1e7c28e` (chore)
2. **Task 2: Rewrite db.py to single Postgres engine and consolidate main.py lifespan** - `aec80f8` (feat)

## Files Created/Modified
- `docker-compose.yml` - Postgres 17 container with healthcheck, credentials, named volume
- `.env.example` - DATABASE_URL template for postgresql+asyncpg
- `src/config.py` - DATABASE_URL fallback changed from sqlite to postgresql+asyncpg
- `requirements.txt` - Added asyncpg, python-dotenv, testcontainers[postgres]; kept aiosqlite
- `src/server/db.py` - Single asyncpg engine with pool settings, removed 3-engine split and all PRAGMA listeners
- `src/server/main.py` - Removed read_engine, set read_session_factory = session_factory (same pool)

## Decisions Made
- Single asyncpg pool replaces read/write engine split — Postgres MVCC handles concurrency, aiosqlite had serialized single-connection-per-engine constraint that required workarounds
- `app.state.read_session_factory` attribute preserved by pointing to same `session_factory` — all API endpoints (players.py, profit.py, portfolio_status.py, portfolio.py) continue to work without modification
- Removed unused `create_session_factory` import from main.py (was only needed for creating read_session_factory from read_engine)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed unused create_session_factory import from main.py**
- **Found during:** Task 2 (main.py lifespan consolidation)
- **Issue:** After removing read_engine, the `create_session_factory` import in main.py became unused (was only needed for `create_session_factory(read_engine)`)
- **Fix:** Removed the unused import from the import line
- **Files modified:** src/server/main.py
- **Verification:** Python import check passes, no NameError
- **Committed in:** aec80f8 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 cleanup bug)
**Impact on plan:** Minor cleanup — unused import removed for correctness. No scope creep.

## Issues Encountered
- Worktree branch was 274 commits behind main (based on early df1b3ea merge commit). Reset worktree branch to main before execution to access server infrastructure files. No code issues.

## User Setup Required
- Run `docker compose up -d` to start Postgres container before running server
- Copy `.env.example` to `.env` if needed (default credentials match docker-compose.yml)

## Next Phase Readiness
- Postgres container definition ready for Plan 02 (schema migration)
- Single-engine db.py foundation established for Plans 02 and 03
- All API endpoint app.state references preserved — no downstream endpoint changes needed

---
*Phase: 09.1-migrate-from-sqlite*
*Completed: 2026-03-28*
