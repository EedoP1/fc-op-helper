---
phase: 09-comprehensive-api-integration-performance-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/server_harness.py
  - tests/integration/conftest.py
  - tests/integration/test_smoke_all_endpoints.py
  - tests/integration/test_performance.py
  - src/config.py
autonomous: true
requirements: [TEST-01, TEST-04]
must_haves:
  truths:
    - "Real uvicorn server starts using real server lifespan (scanner, scheduler, circuit breaker)"
    - "Test DB is a copy of production DB with real scored players and market data"
    - "All 16 API endpoints return non-error responses when hit with valid requests"
    - "Performance thresholds are enforced: health < 100ms, pending < 200ms, status < 300ms, profit < 200ms, portfolio generate < 10s"
  artifacts:
    - path: "tests/integration/server_harness.py"
      provides: "Real-server FastAPI app with TEST_DB_PATH override, real scanner, real scheduler"
      contains: "ScannerService"
    - path: "tests/integration/conftest.py"
      provides: "Session fixtures, DB copy, cleanup, httpx client"
      contains: "live_server"
    - path: "tests/integration/test_smoke_all_endpoints.py"
      provides: "Smoke tests for all 16 endpoints with real DB data"
      contains: "test_"
    - path: "tests/integration/test_performance.py"
      provides: "Latency threshold tests for critical endpoints"
      contains: "elapsed_ms"
    - path: "src/config.py"
      provides: "DATABASE_URL reads from env var with fallback to hardcoded path"
      contains: "os.environ.get"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "tests/integration/server_harness.py"
      via: "uvicorn starts server_harness:app"
      pattern: "server_harness:app"
    - from: "tests/integration/server_harness.py"
      to: "src/server/main.py"
      via: "imports real ScannerService, CircuitBreaker, all routers"
      pattern: "from src.server"
---

<objective>
Replace the mock-based test harness with a real-server harness that starts actual scanner, scheduler, and circuit breaker. Make DATABASE_URL configurable via env var. Write smoke tests for all 16 endpoints using real DB data, and performance threshold tests.

Purpose: Foundation for all integration tests. Without a real server harness and env-configurable DB, no subsequent test can exercise real behavior. The smoke tests verify every endpoint is reachable and returns valid data from real DB. Performance tests catch regressions early.

Output: Working test infrastructure + smoke + perf tests running against real server with real production DB copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-CONTEXT.md
@.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-RESEARCH.md

@src/server/main.py
@src/server/db.py
@src/config.py
@src/server/api/health.py
@src/server/api/players.py
@src/server/api/portfolio.py
@src/server/api/actions.py
@src/server/api/profit.py
@src/server/api/portfolio_status.py
@src/server/models_db.py
@tests/integration/conftest.py
@tests/integration/server_harness.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/server/main.py — real lifespan creates:
- engine, session_factory via create_engine_and_tables()
- CircuitBreaker()
- ScannerService(session_factory, circuit_breaker)
- read_engine via create_read_engine()
- api_write_engine via create_api_write_engine()
- scheduler via create_scheduler(scanner)
- app.state: engine, session_factory (api write), scanner_session_factory, read_session_factory, scanner, circuit_breaker, scheduler

From src/config.py — DATABASE_URL currently hardcoded:
DATABASE_URL = "sqlite+aiosqlite:///D:/op-seller/op_seller.db"

From src/server/db.py — all engine creators accept db_url param:
create_engine_and_tables(db_url: str = DATABASE_URL)
create_read_engine(db_url: str = DATABASE_URL)
create_api_write_engine(db_url: str = DATABASE_URL)

All 16 endpoints:
- GET  /api/v1/health
- GET  /api/v1/players/top
- GET  /api/v1/players/{ea_id}
- GET  /api/v1/portfolio?budget=N
- POST /api/v1/portfolio/generate
- POST /api/v1/portfolio/confirm
- POST /api/v1/portfolio/swap-preview
- GET  /api/v1/portfolio/confirmed
- DELETE /api/v1/portfolio/{ea_id}?budget=N
- GET  /api/v1/actions/pending
- POST /api/v1/actions/{id}/complete
- POST /api/v1/portfolio/slots
- POST /api/v1/trade-records/direct
- POST /api/v1/trade-records/batch
- GET  /api/v1/portfolio/status
- GET  /api/v1/profit/summary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make DATABASE_URL env-configurable and rewrite server_harness.py to use real server</name>
  <files>src/config.py, tests/integration/server_harness.py, tests/integration/conftest.py</files>
  <read_first>src/config.py, src/server/main.py, src/server/db.py, tests/integration/server_harness.py, tests/integration/conftest.py</read_first>
  <action>
1. **src/config.py** — Change the DATABASE_URL line from hardcoded to env-configurable:
   ```python
   import os
   DATABASE_URL = os.environ.get("DATABASE_URL", "sqlite+aiosqlite:///D:/op-seller/op_seller.db")
   ```
   Add `import os` at top if not present. This is the ONLY change to this file. All other constants stay the same.

2. **tests/integration/server_harness.py** — COMPLETE REWRITE. Delete the MockScanner, MockCircuitBreaker, and the simplified lifespan. Replace with a harness that is essentially identical to src/server/main.py but reads TEST_DB_PATH from env to set DATABASE_URL. Per D-01: use the REAL ScannerService, REAL CircuitBreaker, REAL scheduler.

   The harness must:
   - Read TEST_DB_PATH from os.environ (set by conftest before uvicorn starts)
   - Set os.environ["DATABASE_URL"] to `sqlite+aiosqlite:///{TEST_DB_PATH}` BEFORE importing from src.server modules (since config.py reads env at import time)
   - Import and use the REAL lifespan from src.server.main OR replicate it identically
   - Actually, the cleanest approach: set the env var, then import and re-export the real app:
     ```python
     import os
     db_path = os.environ["TEST_DB_PATH"]
     os.environ["DATABASE_URL"] = f"sqlite+aiosqlite:///{db_path}"
     from src.server.main import app  # noqa: E402
     ```
   - This way the REAL server code runs: real scanner, real scheduler, real circuit breaker, real lifespan. The only difference is the DB path.
   - Per D-03: if scanner needs fut.gg API and it's not available, the test should FAIL. Do NOT catch or suppress scanner errors.

   NOTE on subprocess isolation: server_harness.py runs in a FRESH subprocess spawned by conftest via `Popen(["uvicorn", "tests.integration.server_harness:app", ...])`. The env var TEST_DB_PATH is passed via the subprocess environment, so setting os.environ["DATABASE_URL"] in server_harness.py at module load time affects only the server subprocess, not the test process. Add a comment in server_harness.py explaining this isolation model.

3. **tests/integration/conftest.py** — Update the live_server fixture:
   - Keep the DB copy logic (copy D:/op-seller/op_seller.db to temp). Per D-02.
   - Keep the synchronous uvicorn Popen approach (per Phase 09 decision about pytest-asyncio 1.3.0).
   - Change the uvicorn module target from `tests.integration.server_harness:app` — keep this the same, but the harness now starts the real server.
   - Increase readiness poll timeout from 10s to 30s — real server startup with scanner + scheduler takes longer.
   - Keep the per-test cleanup fixture (DELETE FROM trade_records, trade_actions, portfolio_slots per D-15).
   - Keep read-only data preserved (per D-16).
   - Client timeout stays at 30s per D-19.
   - Add a `seed_real_portfolio_slot` fixture that seeds a slot using a REAL ea_id from the production DB (query the test DB to find one). This replaces the fake ea_id=100 fixture which won't work with real data.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -c "import os; os.environ['DATABASE_URL']='sqlite+aiosqlite:///test.db'; from src.config import DATABASE_URL; assert 'test.db' in DATABASE_URL, f'Expected test.db in {DATABASE_URL}'"</automated>
  </verify>
  <acceptance_criteria>
    - src/config.py contains `os.environ.get("DATABASE_URL"` (grep-verifiable)
    - tests/integration/server_harness.py contains `ScannerService` or `from src.server.main import app` (grep-verifiable)
    - tests/integration/server_harness.py does NOT contain `MockScanner` (grep-verifiable)
    - tests/integration/server_harness.py contains a comment explaining subprocess isolation
    - tests/integration/conftest.py readiness poll allows at least 30s
    - tests/integration/conftest.py cleanup deletes trade_records, trade_actions, portfolio_slots
  </acceptance_criteria>
  <done>Real server starts via harness with no mocks. DATABASE_URL is env-configurable. conftest copies real production DB and cleans up mutable tables between tests.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke tests for all 16 endpoints + performance threshold tests</name>
  <files>tests/integration/test_smoke_all_endpoints.py, tests/integration/test_performance.py</files>
  <read_first>tests/integration/conftest.py, src/server/api/health.py, src/server/api/players.py, src/server/api/portfolio.py, src/server/api/actions.py, src/server/api/profit.py, src/server/api/portfolio_status.py</read_first>
  <action>
1. **tests/integration/test_smoke_all_endpoints.py** — Write one test per endpoint that verifies it returns a valid response with real DB data. These are NOT happy-path tests with fake data — they exercise real code paths with real scored players.

   Tests (all use the `client` fixture from conftest, which hits the real running server):

   ```
   test_health_returns_scanner_status — GET /health, assert 200, assert "scanner_status" in body, assert "circuit_breaker" in body
   test_top_players_returns_data — GET /players/top?limit=10, assert 200, assert body["count"] > 0 (real DB has scored players), assert each player has ea_id, name, price
   test_player_detail_real_player — GET /players/{ea_id} using a real ea_id from the DB (query conftest fixture), assert 200, assert body has current_score
   test_player_detail_nonexistent — GET /players/999999999, assert 404
   test_portfolio_generate_real_budget — POST /portfolio/generate with {"budget": 200000}, assert 200, assert body["count"] > 0 (real scored players exist), assert body["budget_used"] > 0
   test_portfolio_get_real_budget — GET /portfolio?budget=200000, assert 200, assert body["count"] > 0
   test_portfolio_confirm — POST /portfolio/confirm with real players from generate response, assert 200, assert body["confirmed"] > 0
   test_portfolio_confirmed_after_confirm — GET /portfolio/confirmed after confirming, assert 200, assert body["count"] > 0
   test_portfolio_swap_preview — POST /portfolio/swap-preview with freed_budget and excluded_ea_ids, assert 200
   test_portfolio_delete — DELETE /portfolio/{ea_id}?budget=200000 after confirming a player, assert 200
   test_portfolio_delete_nonexistent — DELETE /portfolio/999999999?budget=200000, assert 404
   test_actions_pending_empty — GET /actions/pending with no portfolio, assert 200, assert body["action"] is None
   test_actions_pending_with_slot — POST /portfolio/slots to seed, then GET /actions/pending, assert body["action"] is not None
   test_actions_complete — complete an action from pending, assert 200
   test_seed_portfolio_slots — POST /portfolio/slots with real data, assert 201
   test_trade_records_direct — POST /trade-records/direct after seeding slot, assert 201. NOTE: This is a smoke test (happy path only). Partial failure and edge cases for trade records are tested in Plan 02 (test_lifecycle_flows.py — test_batch_trade_records_mixed) and Plan 03 (test_edge_cases.py — test_direct_trade_record_invalid_outcome, test_direct_trade_record_not_in_portfolio).
   test_trade_records_batch — POST /trade-records/batch after seeding slots, assert 201. NOTE: Smoke test only — partial failure with mixed valid/invalid ea_ids is tested in Plan 02 (test_lifecycle_flows.py — test_batch_trade_records_mixed).
   test_portfolio_status_empty — GET /portfolio/status with no slots, assert 200
   test_profit_summary_empty — GET /profit/summary with no records, assert 200
   ```

   IMPORTANT per D-04: Tests that fail = server bugs. Write STRICT assertions. Do NOT weaken assertions to make tests pass. If portfolio/generate returns empty data when real scored players exist, that's a server bug to fix.

   For tests that need a real ea_id: add a helper fixture `real_ea_id` that queries the test DB directly (via aiosqlite) to find an active player with a viable score. This avoids hardcoding ea_ids.

2. **tests/integration/test_performance.py** — Performance threshold tests per D-13. Each test:
   - Issues one warmup request (to initialize connection pool)
   - Times 5 consecutive requests using time.perf_counter()
   - Takes the p95 (worst of 5)
   - Asserts against threshold

   ```
   test_health_latency — GET /health, p95 < 100ms (D-13)
   test_pending_action_latency — GET /actions/pending, p95 < 200ms (D-13)
   test_portfolio_status_latency — GET /portfolio/status, p95 < 300ms (D-13, needs seeded slots)
   test_profit_summary_latency — GET /profit/summary, p95 < 200ms (D-13)
   test_portfolio_generate_latency — POST /portfolio/generate with {"budget": 200000}, measure time, assert < 10s (D-14 — "if generating takes too long, that's a bug"). Portfolio generation with real scored data in the 11k-200k range should complete well under 10 seconds; exceeding this threshold indicates a performance regression.
   ```

   Use `time.perf_counter()` for timing (stdlib, no extra deps needed). Per D-04: if a threshold fails, that's a server performance bug — do NOT weaken the threshold.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_smoke_all_endpoints.py tests/integration/test_performance.py -x --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_smoke_all_endpoints.py contains at least 15 test functions (grep -c "async def test_" returns >= 15)
    - tests/integration/test_performance.py contains at least 5 test functions testing latency
    - test_performance.py contains "elapsed_ms" and threshold assertions (100, 200, 300)
    - test_performance.py portfolio generate threshold is 10s (10000ms), NOT 30s
    - test_smoke_all_endpoints.py does NOT contain "MockScanner" or "mock" (no mocking)
    - test_smoke_all_endpoints.py exercises all endpoint prefixes: /health, /players, /portfolio, /actions, /profit, /trade-records
  </acceptance_criteria>
  <done>All 16 endpoints have smoke tests running against real server with real DB data. Performance thresholds enforced for critical endpoints (including portfolio generate < 10s per D-14). Tests that fail indicate real server bugs.</done>
</task>

</tasks>

<verification>
Run full integration test collection:
```bash
cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/ --collect-only
```
Should show 20+ tests collected from test_smoke_all_endpoints.py and test_performance.py.

Run actual tests (requires real DB at D:/op-seller/op_seller.db):
```bash
cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/ -x -v --timeout=120
```
</verification>

<success_criteria>
- Real server (with scanner, scheduler, circuit breaker) starts via test harness
- Production DB copied to temp file, real data available
- All 16 endpoints exercised with real data
- Performance thresholds from D-13 codified as assertions
- Portfolio generate threshold < 10s per D-14
- No mocks anywhere in integration test code
</success_criteria>

<output>
After completion, create `.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-01-SUMMARY.md`
</output>
