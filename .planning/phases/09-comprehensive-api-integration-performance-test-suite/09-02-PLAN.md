---
phase: 09-comprehensive-api-integration-performance-test-suite
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/integration/test_lifecycle_flows.py
  - tests/integration/test_concurrent_removes.py
  - tests/integration/test_scanner_interaction.py
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "Full BUY->LIST->SOLD cycle works end-to-end through the real server"
    - "EXPIRED->RELIST cycle correctly derives RELIST action from trade records"
    - "Removing 2+ players concurrently does NOT produce duplicate replacements"
    - "Portfolio status and profit summary reflect the correct state after lifecycle events"
    - "Batch trade records with mixed valid/invalid ea_ids handles partial failures correctly"
    - "Health endpoint reflects real scanner state (running/idle) and read endpoints respond while scanner holds DB locks"
  artifacts:
    - path: "tests/integration/test_lifecycle_flows.py"
      provides: "Cross-endpoint lifecycle flow tests"
      contains: "test_full_buy_list_sold_cycle"
    - path: "tests/integration/test_concurrent_removes.py"
      provides: "Concurrent access pattern tests"
      contains: "test_concurrent_remove"
    - path: "tests/integration/test_scanner_interaction.py"
      provides: "Scanner interaction and DB lock tests per D-12"
      contains: "test_health_reflects_scanner_state"
  key_links:
    - from: "tests/integration/test_lifecycle_flows.py"
      to: "/api/v1/actions/pending -> /api/v1/actions/{id}/complete -> /api/v1/portfolio/status"
      via: "sequential HTTP calls through full lifecycle"
      pattern: "actions/pending.*actions.*complete.*portfolio/status"
    - from: "tests/integration/test_concurrent_removes.py"
      to: "/api/v1/portfolio/{ea_id}"
      via: "asyncio.gather of concurrent DELETE requests"
      pattern: "asyncio.gather"
    - from: "tests/integration/test_scanner_interaction.py"
      to: "/api/v1/health"
      via: "verifies scanner_status reflects real scanner state"
      pattern: "scanner_status"
---

<objective>
Test the real-world workflows the user actually does: full trade lifecycles (BUY->LIST->SOLD, EXPIRED->RELIST, multi-cycle), concurrent removes that trigger the known duplicate-player bug, cross-endpoint state consistency, and scanner interaction behavior (D-12). These tests are designed to find bugs, not confirm correctness.

Purpose: The user has experienced specific bugs (duplicate players on concurrent removes, slow portfolio generation, fragile player insertion). These tests exercise those exact patterns against the real server with real data. Per D-04: tests that fail = server bugs to fix. D-12 scanner interaction tests verify API behavior during different scanner states and DB lock conditions.

Output: Lifecycle flow tests, concurrent access tests, and scanner interaction tests that expose real server bugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-CONTEXT.md
@.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-01-SUMMARY.md

@src/server/api/actions.py
@src/server/api/portfolio.py
@src/server/api/portfolio_status.py
@src/server/api/profit.py
@src/server/api/health.py
@src/server/models_db.py
@src/health_check.py
@tests/integration/conftest.py

<interfaces>
<!-- Endpoint contracts the executor needs for lifecycle flow tests -->

POST /api/v1/portfolio/slots — seed slots
  Request: {"slots": [{"ea_id": int, "buy_price": int, "sell_price": int, "player_name": str}]}
  Response 201: {"status": "ok", "count": int}

GET /api/v1/actions/pending — get next action
  Response 200: {"action": {"id": int, "ea_id": int, "action_type": str, "target_price": int, "player_name": str} | null}

POST /api/v1/actions/{id}/complete — complete action
  Request: {"price": int, "outcome": str}  # "bought"|"listed"|"sold"|"expired"
  Response 200: {"status": "ok", "trade_record_id": int}

POST /api/v1/portfolio/confirm — confirm portfolio
  Request: {"players": [{"ea_id": int, "buy_price": int, "sell_price": int}]}
  Response 200: {"confirmed": int, "status": "ok"}

DELETE /api/v1/portfolio/{ea_id}?budget=N — remove player and get replacements
  Response 200: {"removed_ea_id": int, "freed_budget": int, "replacements": [...]}

POST /api/v1/trade-records/direct — direct trade record
  Request: {"ea_id": int, "price": int, "outcome": str}
  Response 201: {"status": "ok", "trade_record_id": int}

POST /api/v1/trade-records/batch — batch trade records
  Request: {"records": [{"ea_id": int, "price": int, "outcome": str}]}
  Response 201: {"status": "ok", "succeeded": [int], "failed": [int]}

GET /api/v1/portfolio/status — portfolio status with per-player trade info
  Response 200: {"summary": {...}, "players": [{...}]}

GET /api/v1/profit/summary — profit aggregation
  Response 200: {"totals": {"total_spent": int, "total_earned": int, "net_profit": int, "trade_count": int}, "per_player": [...]}

GET /api/v1/health — health check with scanner state
  Response 200: {"scanner_status": str, "circuit_breaker": str, "players_in_db": int, ...}

Lifecycle state machine (from _derive_next_action):
  No records     -> BUY
  bought         -> LIST
  listed         -> waiting (skip)
  sold           -> BUY (restart)
  expired        -> RELIST
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-endpoint lifecycle flow tests</name>
  <files>tests/integration/test_lifecycle_flows.py</files>
  <read_first>src/server/api/actions.py, src/server/api/portfolio.py, src/server/api/portfolio_status.py, src/server/api/profit.py, tests/integration/conftest.py</read_first>
  <action>
Create tests/integration/test_lifecycle_flows.py with comprehensive cross-endpoint lifecycle tests. Each test exercises multiple endpoints sequentially to verify the real server maintains correct state across the full trade lifecycle.

All tests use the `client` fixture from conftest (real HTTP to real server). Use real ea_ids from the test DB via a helper that queries portfolio-eligible players.

**Helper fixture:** `real_ea_ids(client)` — call POST /portfolio/generate with budget=200000, extract 3 ea_ids from the response data. If generate returns empty (no scored players), skip the test. These are REAL players with real scores.

**Tests to write (each is independent — cleanup fixture resets mutable tables):**

1. `test_full_buy_list_sold_cycle` (D-07, D-10):
   - Seed 1 slot (POST /portfolio/slots with real ea_id, buy_price, sell_price from generate response)
   - GET /actions/pending -> assert action_type == "BUY", extract action_id
   - POST /actions/{id}/complete with outcome="bought", price=buy_price
   - GET /actions/pending -> assert action_type == "LIST", extract action_id
   - POST /actions/{id}/complete with outcome="sold", price=sell_price
   - GET /actions/pending -> assert action_type == "BUY" (new cycle starts per lifecycle state machine)
   - GET /portfolio/status -> assert player status == "SOLD" (most recent record)
   - Wait, after sold the next pending should be BUY. But portfolio/status reads latest TradeRecord. After completing with sold, latest record is "sold" -> status "SOLD". Then next pending derives BUY (since sold -> restart cycle).
   - GET /profit/summary -> assert totals.net_profit != 0 (there was a sale)

2. `test_buy_list_expired_relist_cycle` (D-10):
   - Seed 1 slot
   - BUY -> complete(bought) -> LIST -> complete(expired) ->
   - GET /actions/pending -> assert action_type == "RELIST"
   - complete(listed) -> GET /actions/pending -> action is None (card is "listed" = on market)

3. `test_listed_means_waiting` (D-10):
   - Seed 1 slot
   - BUY -> complete(bought) -> LIST -> complete(listed)
   - GET /actions/pending -> assert action is None (card on market, nothing to do)

4. `test_direct_trade_record_advances_lifecycle`:
   - Seed 1 slot
   - POST /trade-records/direct with outcome="bought" for that ea_id
   - GET /actions/pending -> assert action_type == "LIST" (direct record advanced lifecycle)

5. `test_direct_trade_record_deduplication`:
   - Seed 1 slot
   - POST /trade-records/direct with outcome="bought" twice for same ea_id
   - Second call should return deduplicated=True, trade_record_id=-1

6. `test_batch_trade_records_mixed`:
   - Seed 2 slots with real ea_ids
   - POST /trade-records/batch with 3 records: 2 valid ea_ids (bought), 1 invalid ea_id (999999999)
   - Assert succeeded contains the 2 valid ea_ids, failed contains 999999999

7. `test_batch_trade_records_dedup`:
   - Seed 1 slot, POST /trade-records/direct with outcome="bought"
   - POST /trade-records/batch with same ea_id and outcome="bought"
   - Assert succeeded (deduped = success), verify no duplicate TradeRecord created
   - GET /actions/pending -> should still derive LIST (not confused by dedup)

8. `test_portfolio_status_reflects_lifecycle` (D-07):
   - Seed 2 slots
   - BUY+complete(bought) for slot 1
   - BUY+complete(bought)+LIST+complete(sold) for slot 2
   - GET /portfolio/status
   - Assert slot 1 status == "BOUGHT", slot 2 status == "SOLD"
   - Assert summary.trade_counts.bought >= 2, sold >= 1

9. `test_profit_summary_after_full_cycle`:
   - Seed 1 slot (buy_price=50000, sell_price=70000)
   - Full cycle: BUY(50000) -> bought -> LIST -> sold(70000)
   - GET /profit/summary
   - Assert totals.total_spent == 50000
   - Assert totals.total_earned == int(70000 * 0.95) (EA 5% tax)
   - Assert totals.net_profit == int(70000 * 0.95) - 50000

10. `test_confirm_then_lifecycle`:
    - POST /portfolio/generate with budget=200000
    - POST /portfolio/confirm with the generated players
    - GET /portfolio/confirmed -> assert count matches
    - GET /actions/pending -> assert action is not None (should derive BUY for first slot)

CRITICAL per D-04: Do NOT weaken assertions. If the server returns unexpected data, the test FAILS and that's a bug to fix. Do NOT add try/except or fallback logic in tests.

Per D-05: These are real-world workflows. The extension does exactly this sequence.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_lifecycle_flows.py -x --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_lifecycle_flows.py contains at least 9 test functions (grep -c "async def test_" >= 9)
    - File contains "BUY" and "LIST" and "RELIST" string assertions (lifecycle state machine coverage)
    - File contains "/actions/pending" and "/actions/" and "/complete" (cross-endpoint flow)
    - File contains "/portfolio/status" (status verification after lifecycle events)
    - File contains "/profit/summary" (profit verification after trade cycles)
    - File contains "/trade-records/batch" (batch endpoint coverage)
    - File does NOT contain "mock" or "Mock" (no mocking)
  </acceptance_criteria>
  <done>10 lifecycle flow tests exercise the full BUY->LIST->SOLD, EXPIRED->RELIST, direct records, batch records, status reflection, and profit calculation through the real server.</done>
</task>

<task type="auto">
  <name>Task 2: Concurrent access, duplicate detection, and scanner interaction tests</name>
  <files>tests/integration/test_concurrent_removes.py, tests/integration/test_scanner_interaction.py</files>
  <read_first>src/server/api/portfolio.py, src/server/api/actions.py, src/server/api/health.py, src/health_check.py, tests/integration/conftest.py</read_first>
  <action>
**Part A: tests/integration/test_concurrent_removes.py** — targeting the known concurrent-remove duplicate bug and other race conditions. These tests use asyncio.gather to fire multiple requests simultaneously.

**Tests to write:**

1. `test_concurrent_remove_two_players_no_duplicates` (D-08 — the known bug):
   - POST /portfolio/generate with budget=200000, get player list
   - POST /portfolio/confirm with the generated players
   - Pick 2 players from the confirmed list
   - Fire 2 concurrent DELETE /portfolio/{ea_id}?budget=200000 requests using asyncio.gather
   - Both should return 200
   - Collect all replacement ea_ids from both responses
   - GET /portfolio/confirmed — get current portfolio
   - Assert NO duplicate ea_ids in the portfolio (this is where the bug manifests)
   - Assert the 2 removed players are NOT in the portfolio
   - Assert total portfolio count is reasonable (original count - 2 + replacements)

2. `test_concurrent_remove_three_players` (D-08 extended):
   - Same as above but with 3 concurrent removes
   - More likely to trigger race conditions

3. `test_rapid_pending_action_polling` (D-11):
   - Seed 1 slot
   - Fire 10 concurrent GET /actions/pending using asyncio.gather
   - All should return 200
   - At most 1 should have a non-null action (action should be claimed by exactly one)
   - Actually, the real behavior: the first call derives and claims the action. Subsequent concurrent calls should either see the IN_PROGRESS action or return null. Due to SQLite serialization, they may all see the same IN_PROGRESS action (idempotent return). Assert: all responses are valid (200), all non-null actions have the same id (no duplicate actions created).

4. `test_rapid_complete_same_action` (D-11):
   - Seed 1 slot, get pending action
   - Fire 3 concurrent POST /actions/{id}/complete with outcome="bought"
   - First should succeed (200), subsequent may return 200 too (since complete doesn't guard against re-completing per Phase 09 decision)
   - Key assertion: GET /actions/pending after should derive LIST (not corrupt state)
   - GET /portfolio/status should show BOUGHT status (not inconsistent)

5. `test_concurrent_slot_seeding` (D-11):
   - Fire 5 concurrent POST /portfolio/slots with the same ea_id
   - Should not create duplicate PortfolioSlot rows (ea_id is UNIQUE)
   - GET /portfolio/confirmed should show at most 1 entry for that ea_id

6. `test_concurrent_direct_trade_records` (D-11):
   - Seed 1 slot
   - Fire 5 concurrent POST /trade-records/direct with same ea_id and outcome="bought"
   - Server-side dedup should prevent duplicate records
   - Only 1 trade record should be created (subsequent calls deduped)
   - GET /actions/pending should correctly derive LIST

7. `test_remove_during_action_lifecycle` (D-10):
   - Seed 2 slots
   - GET /actions/pending -> get BUY action for slot 1
   - While action is IN_PROGRESS, DELETE /portfolio/{ea_id} for slot 1
   - The delete should cancel the pending/in-progress action
   - GET /actions/pending should now return action for slot 2 (not the cancelled one)

IMPORTANT per D-06: Concurrent access patterns are critical. The real extension sends rapid API calls. Use `asyncio.gather` with real httpx.AsyncClient connections to the real server.

Per D-04: Tests that fail = server bugs. The concurrent remove duplicate bug is a KNOWN issue. If test_concurrent_remove_two_players_no_duplicates fails, that confirms the bug exists and needs fixing.

**Part B: tests/integration/test_scanner_interaction.py** — Per D-12: test API behavior when scanner is running, idle, or mid-scan, and verify read endpoints respond while scanner holds DB locks.

**Tests to write:**

1. `test_health_reflects_real_scanner_state` (D-12):
   - GET /health immediately after server starts
   - Assert scanner_status is a real state string ("running", "idle", "scanning", or similar — NOT "mock" or "unknown")
   - Assert the response includes last_scan_time or scan_count (evidence the real scanner is integrated)
   - Call GET /health multiple times over 2-3 seconds, verify scanner_status is consistent and not flickering randomly

2. `test_read_endpoints_respond_during_scanner_activity` (D-12):
   - The real scanner is running in the background (started by server lifespan)
   - Fire a burst of read requests concurrently using asyncio.gather:
     - GET /health
     - GET /players/top?limit=5
     - GET /portfolio/status
     - GET /profit/summary
     - GET /actions/pending
   - ALL must return 200 (not 500 from DB lock errors)
   - Repeat this burst 3 times with 1-second gaps to increase chance of overlapping with scanner DB writes
   - If any request returns 500 with a "database is locked" message, that's a real bug (WAL mode should prevent this)

3. `test_write_endpoints_respond_during_scanner_activity` (D-12):
   - Seed a portfolio slot
   - Fire write requests while scanner may be active:
     - POST /trade-records/direct
     - GET /actions/pending (creates IN_PROGRESS action — involves write)
   - Assert both succeed (200/201), not blocked by scanner DB locks
   - This tests the multi-engine architecture (scanner write engine vs API write engine)

Per D-03: If the real scanner fails to start (e.g., fut.gg unreachable), the server startup should fail and the test should fail. Do NOT add workarounds.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_concurrent_removes.py tests/integration/test_scanner_interaction.py -x --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_concurrent_removes.py contains at least 6 test functions (grep -c "async def test_" >= 6)
    - File contains "asyncio.gather" (concurrent request pattern)
    - File contains "duplicate" or "no_duplicate" in test names or assertions
    - File contains "DELETE" and "/portfolio/" (concurrent remove tests)
    - File contains "/actions/pending" (race condition on action queue)
    - tests/integration/test_scanner_interaction.py contains at least 3 test functions (grep -c "async def test_" >= 3)
    - test_scanner_interaction.py contains "scanner_status" (verifies real scanner state)
    - test_scanner_interaction.py contains "database is locked" or "locked" (checks for DB lock errors)
    - Neither file contains "mock" or "Mock"
  </acceptance_criteria>
  <done>7 concurrent access tests exercise the known duplicate-player bug, rapid polling races, concurrent completions, and remove-during-lifecycle scenarios. 3 scanner interaction tests verify health reflects real scanner state and that read/write endpoints respond during scanner activity (D-12). Tests use asyncio.gather against the real server.</done>
</task>

</tasks>

<verification>
Run full integration test collection:
```bash
cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_lifecycle_flows.py tests/integration/test_concurrent_removes.py tests/integration/test_scanner_interaction.py --collect-only
```
Should show 20+ tests collected.

Run actual tests:
```bash
cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/ -x -v --timeout=120
```
</verification>

<success_criteria>
- Full trade lifecycle tested end-to-end through real HTTP
- Known concurrent-remove duplicate bug has a test (may fail — that confirms the bug)
- Rapid concurrent access patterns tested
- Cross-endpoint state consistency verified (status + profit reflect lifecycle events)
- Batch trade records with partial failures tested
- Scanner interaction tested: health reflects real state, endpoints respond during scanner activity (D-12)
- No mocks anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-02-SUMMARY.md`
</output>
