---
phase: 09-comprehensive-api-integration-performance-test-suite
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/integration/test_endpoint_edge_cases.py
  - tests/integration/test_error_handling.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Invalid JSON bodies return 422 with descriptive errors"
    - "Non-existent resource IDs return 404"
    - "Missing required fields return 422"
    - "Boundary values (negative budget, zero budget, huge IDs) are handled gracefully"
    - "CORS headers are present on responses from chrome-extension origin"
    - "Duplicate portfolio confirms replace cleanly (idempotent)"
    - "Batch trade records with mixed valid/invalid ea_ids return partial success"
    - "Direct trade record deduplication works (same outcome twice returns deduplicated=true)"
  artifacts:
    - path: "tests/integration/test_endpoint_edge_cases.py"
      provides: "Deep edge case tests for all endpoints — boundary values, empty inputs, duplicates"
      contains: "async def test_"
    - path: "tests/integration/test_error_handling.py"
      provides: "Error handling tests — bad JSON, invalid types, missing fields, CORS"
      contains: "async def test_"
  key_links:
    - from: "tests/integration/test_endpoint_edge_cases.py"
      to: "tests/integration/conftest.py"
      via: "client and cleanup_tables fixtures"
      pattern: "async def test_.*client"
---

<objective>
Deep endpoint testing with edge cases, error handling, and CORS validation for all 16 API endpoints.

Purpose: Smoke tests (Plan 01) prove endpoints respond. This plan verifies they handle bad input, missing data, boundary values, duplicate operations, and CORS correctly. These are the tests that catch regressions in input validation and error paths.

NOTE: All tests use real HTTP to a real uvicorn server (not ASGI transport) per explicit user requirement.

Output: Two test files covering edge cases and error handling across all endpoints.
</objective>

<execution_context>
@C:\Users\maftu\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\maftu\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-01-SUMMARY.md
@src/server/api/players.py
@src/server/api/health.py
@src/server/api/portfolio.py
@src/server/api/actions.py
@src/server/api/profit.py
@src/server/api/portfolio_status.py
@tests/integration/conftest.py

<interfaces>
<!-- From conftest.py (Plan 01): -->
Fixtures available:
- `client` — httpx.AsyncClient with base_url pointed at real uvicorn server (function-scoped, async)
- `live_server` — session-scoped, auto-used, SYNCHRONOUS fixture that starts/stops uvicorn subprocess
- `cleanup_tables` — auto-used, deletes from trade_records, trade_actions, portfolio_slots after each test
- `test_db_path` — path to real SQLite file
- `seed_portfolio_slot` — seeds ea_id=100 slot

Endpoint validation rules (from source code):
- GET /portfolio requires `budget` query param (gt=0)
- POST /portfolio/generate requires JSON body with `budget` (gt=0)
- POST /portfolio/confirm requires JSON body with `players` list of {ea_id, buy_price, sell_price}
- POST /portfolio/swap-preview requires JSON body with `freed_budget` (gt=0) and `excluded_ea_ids` list
- DELETE /portfolio/{ea_id} requires `budget` query param (gt=0)
- POST /actions/{id}/complete requires JSON body with `price` (int) and `outcome` (str)
- POST /portfolio/slots requires JSON body with `slots` list of {ea_id, buy_price, sell_price, player_name}
- POST /trade-records/direct requires JSON body with `ea_id`, `price`, `outcome`
- POST /trade-records/batch requires JSON body with `records` list
- Valid outcomes: "bought", "listed", "sold", "expired"
- CORS: allow_origin_regex=r"chrome-extension://.*"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edge case tests for all endpoints — boundaries, duplicates, empty inputs</name>
  <files>tests/integration/test_endpoint_edge_cases.py</files>
  <read_first>
    - tests/integration/conftest.py (available fixtures and cleanup mechanism)
    - src/server/api/portfolio.py (confirm dedup logic, generate empty DB, swap-preview stateless)
    - src/server/api/actions.py (pending derivation, stale reset, batch dedup, direct dedup)
    - src/server/api/profit.py (empty state response)
    - src/server/api/portfolio_status.py (empty state, status derivation from latest outcome)
    - src/server/api/players.py (top players pagination, price filters, player detail 404)
  </read_first>
  <action>
    Create `tests/integration/test_endpoint_edge_cases.py` with real HTTP tests covering:

    **Players edge cases:**
    - `test_top_players_with_limit_and_offset` — GET /players/top?limit=5&offset=0 returns valid pagination shape
    - `test_top_players_price_filter` — GET /players/top?price_min=1000&price_max=50000 returns empty (no scored players in test DB)
    - `test_top_players_limit_max_500` — GET /players/top?limit=501 returns 422 (le=500 validation)
    - `test_player_detail_negative_id` — GET /players/-1 returns 404 or valid empty

    **Portfolio edge cases:**
    - `test_portfolio_budget_zero` — GET /portfolio?budget=0 returns 422 (gt=0)
    - `test_portfolio_budget_negative` — GET /portfolio?budget=-1 returns 422
    - `test_generate_budget_zero` — POST /portfolio/generate with budget=0 returns 422
    - `test_confirm_empty_players_list` — POST /portfolio/confirm with {"players": []} returns 200 with confirmed=0
    - `test_confirm_duplicate_ea_ids` — POST /portfolio/confirm with two entries having same ea_id; should deduplicate, confirmed=1
    - `test_confirm_twice_replaces` — POST /portfolio/confirm, then POST again with different players; GET /portfolio/confirmed should only show second set
    - `test_swap_preview_empty_excluded` — POST /portfolio/swap-preview with excluded_ea_ids=[] works
    - `test_swap_preview_freed_budget_zero` — returns 422 (gt=0)

    **Actions edge cases:**
    - `test_pending_action_with_slot` — seed slot via /portfolio/slots, then GET /actions/pending; should return BUY action
    - `test_pending_action_buy_then_list` — seed slot, get BUY action, complete with "bought", get next pending; should be LIST
    - `test_complete_action_already_done` — complete same action twice; second call should still succeed (action already DONE, but endpoint may return 404 or handle gracefully)
    - `test_seed_slots_empty_list` — POST /portfolio/slots with empty slots list returns 200 (not 201)
    - `test_seed_slots_update_existing` — seed slot, then seed same ea_id with different prices; prices should update

    **Trade records edge cases:**
    - `test_direct_trade_record_valid` — seed slot, then POST /trade-records/direct with matching ea_id; returns 201
    - `test_direct_trade_record_dedup` — POST same outcome twice for same ea_id; second returns deduplicated=true
    - `test_direct_trade_record_invalid_outcome` — POST with outcome="invalid" returns 400
    - `test_batch_mixed_valid_invalid` — seed one slot (ea_id=100), batch with ea_id=100 (valid) + ea_id=999 (invalid); succeeded=[100], failed=[999]
    - `test_batch_dedup_within_batch` — batch two records for same ea_id with same outcome; first succeeds, second is deduped

    **Profit edge cases:**
    - `test_profit_with_trade_data` — seed slot, complete BUY+LIST cycle via actions, then GET /profit/summary; totals should show trade_count > 0

    **Portfolio status edge cases:**
    - `test_status_with_pending_slot` — seed slot, GET /portfolio/status; player should show status="PENDING"
    - `test_status_after_buy` — seed slot, complete BUY action, GET /portfolio/status; player should show status="BOUGHT"

    Each test must be self-contained: seed its own data via API calls, assert on the response, and rely on the autouse cleanup fixture.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_endpoint_edge_cases.py -x -v --timeout=60 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_endpoint_edge_cases.py contains at least 20 `async def test_` functions
    - File tests boundary values: budget=0, budget=-1, limit=501, empty lists
    - File tests deduplication: confirm duplicates, direct trade record dedup, batch dedup
    - File tests idempotency: confirm twice, seed slots update
    - File tests action lifecycle: BUY -> LIST progression via real HTTP
    - `python -m pytest tests/integration/test_endpoint_edge_cases.py -x` passes
  </acceptance_criteria>
  <done>All edge case tests pass via real HTTP to real uvicorn server. Boundary values, duplicates, empty inputs, and lifecycle progressions verified.</done>
</task>

<task type="auto">
  <name>Task 2: Error handling and CORS tests</name>
  <files>tests/integration/test_error_handling.py</files>
  <read_first>
    - tests/integration/conftest.py (fixtures)
    - src/server/main.py (CORS middleware config: allow_origin_regex=r"chrome-extension://.*")
    - src/server/api/actions.py (CompleteActionPayload validation, DirectTradeRecordPayload)
    - src/server/api/portfolio.py (GenerateRequest, ConfirmRequest, SwapPreviewRequest validation)
  </read_first>
  <action>
    Create `tests/integration/test_error_handling.py` with:

    **Invalid JSON / malformed requests:**
    - `test_confirm_invalid_json` — POST /portfolio/confirm with body="not json" and Content-Type: application/json; expect 422
    - `test_confirm_missing_required_field` — POST /portfolio/confirm with {"players": [{"ea_id": 100}]} (missing buy_price, sell_price); expect 422
    - `test_generate_missing_budget` — POST /portfolio/generate with {} (empty body); expect 422
    - `test_complete_action_missing_fields` — POST /actions/1/complete with {} expect 422
    - `test_direct_trade_record_missing_ea_id` — POST /trade-records/direct with {"price": 100, "outcome": "bought"} expect 422
    - `test_batch_invalid_record_shape` — POST /trade-records/batch with {"records": [{"invalid": true}]} expect 422

    **Wrong types:**
    - `test_confirm_ea_id_string` — POST /portfolio/confirm with ea_id as string; expect 422
    - `test_portfolio_budget_string` — GET /portfolio?budget=abc; expect 422
    - `test_slots_buy_price_float` — POST /portfolio/slots with buy_price as float; may coerce or 422

    **CORS validation:**
    - `test_cors_chrome_extension_origin` — Send OPTIONS preflight with Origin: chrome-extension://abc123 to /api/v1/health; expect Access-Control-Allow-Origin header matching the extension origin
    - `test_cors_regular_origin_rejected` — Send request with Origin: http://evil.com; response should NOT have Access-Control-Allow-Origin (or it should not match)
    - `test_cors_actual_request` — Send GET /api/v1/health with Origin: chrome-extension://test123; response should include Access-Control-Allow-Origin

    **Non-existent resources:**
    - `test_complete_nonexistent_action` — POST /actions/999999/complete with valid payload; expect 404
    - `test_delete_nonexistent_player` — DELETE /portfolio/999999?budget=1000000; expect 404
    - `test_direct_record_nonexistent_slot` — POST /trade-records/direct with ea_id not in portfolio; expect 404

    **Content type handling:**
    - `test_post_without_content_type` — POST /portfolio/generate without Content-Type header, with JSON body; expect 422 or appropriate error

    For CORS tests, use httpx with explicit headers:
    ```python
    async def test_cors_chrome_extension_origin(client):
        r = await client.options("/api/v1/health", headers={
            "Origin": "chrome-extension://abcdef123456",
            "Access-Control-Request-Method": "GET",
        })
        assert "access-control-allow-origin" in r.headers
        assert "chrome-extension://" in r.headers["access-control-allow-origin"]
    ```
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller && python -m pytest tests/integration/test_error_handling.py -x -v --timeout=60 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_error_handling.py contains at least 15 `async def test_` functions
    - File tests invalid JSON, missing required fields, wrong types for at least 3 different endpoints
    - File tests CORS with chrome-extension:// origin (expects Access-Control-Allow-Origin header)
    - File tests CORS rejection for non-extension origins
    - File tests 404 responses for nonexistent action IDs, player IDs, portfolio entries
    - `python -m pytest tests/integration/test_error_handling.py -x` passes
  </acceptance_criteria>
  <done>Error handling tests pass — invalid input returns appropriate 4xx codes, CORS allows chrome-extension origin, rejects others.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/integration/test_endpoint_edge_cases.py tests/integration/test_error_handling.py -v --timeout=60` — all pass
2. Edge case count: at least 35 tests across both files
3. No test depends on another test's data (cleanup between tests)
</verification>

<success_criteria>
- At least 20 edge case tests covering boundaries, duplicates, idempotency, lifecycle progression
- At least 15 error handling tests covering invalid JSON, missing fields, wrong types, CORS, 404s
- All tests use real HTTP calls to real uvicorn server
- CORS tests verify chrome-extension:// origin is allowed
- All tests pass independently (no ordering dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/09-comprehensive-api-integration-performance-test-suite/09-02-SUMMARY.md`
</output>
