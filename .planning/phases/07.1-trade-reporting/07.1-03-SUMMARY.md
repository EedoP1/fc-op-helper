---
phase: 07.1-trade-reporting
plan: "03"
subsystem: extension
tags: [typescript, chrome-extension, dom, trade-reporting, mutation-observer]

# Dependency graph
requires:
  - phase: 07.1-trade-reporting-plan-01
    provides: extension/src/selectors.ts — centralized selector map for EA Web App DOM
  - phase: 07.1-trade-reporting-plan-02
    provides: POST /api/v1/trade-records/direct backend endpoint
provides:
  - "extension/src/trade-observer.ts — pure DOM reader for Transfer List with readTransferList() + isTransferListPage()"
  - "TRADE_REPORT and TRADE_REPORT_RESULT message types in messages.ts"
  - "reportedOutcomesItem deduplication storage in storage.ts"
  - "handleTradeReport() handler in background.ts proxying to /trade-records/direct"
  - "maybeStartTradeObserver() + scanTransferList() wired into content script"
affects:
  - 07.2-portfolio-dashboard (reads trade records written by this pipeline)
  - 08-dom-automation (builds on same selectors and content script patterns)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure DOM reader function — readTransferList() accepts Document|Element root for testability"
    - "STATUS_MAP + time-remaining detection for normalizing EA DOM status text"
    - "Composite dedup key {ea_id}:{outcome}:{price} for idempotent trade reporting"
    - "MutationObserver scoped to transfer list container with childList+subtree+characterData"
    - "maybeStartTradeObserver() guards activation — only runs when TRANSFER_LIST_CONTAINER present"

key-files:
  created:
    - extension/src/trade-observer.ts
    - extension/tests/trade-observer.test.ts
  modified:
    - extension/src/messages.ts
    - extension/src/storage.ts
    - extension/entrypoints/background.ts
    - extension/entrypoints/ea-webapp.content.ts
    - extension/tests/background.test.ts
    - extension/tests/content.test.ts

key-decisions:
  - "readTransferList() accepts Document|Element root parameter — enables unit testing with jsdom without mocking globals"
  - "isTimeRemaining() function detects EA time strings (e.g. '55 Minutes') as active listings — EA does not use a simple 'Active' string"
  - "TRADE_REPORT/TRADE_REPORT_RESULT added to exhaustive switch in content script immediately when messages.ts extended — prevents TypeScript errors cascading"
  - "Trade observer MutationObserver uses subtree:true + characterData:true — status text updates are characterData mutations within nested elements"

patterns-established:
  - "DOM reader pattern: pure function with injected root — all EA DOM readers should follow this for testability"
  - "Deduplication key pattern: {ea_id}:{outcome}:{price} composite — uniquely identifies events without DB"
  - "Observer cleanup in ctx.onInvalidated — matches existing panel.destroy() and body observer patterns"

requirements-completed:
  - AUTO-08

# Metrics
duration: 10min
completed: 2026-03-27
---

# Phase 07.1 Plan 03: Trade Observer Pipeline Summary

**Complete passive trade observer pipeline: DOM reader + dedup storage + TRADE_REPORT messages + service worker backend relay + MutationObserver-driven content script wiring**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-27T10:37:23Z
- **Completed:** 2026-03-27T10:46:00Z
- **Tasks:** 2 of 2 complete
- **Files modified:** 8 (1 created, 1 test created, 6 modified)

## Accomplishments

- Created `extension/src/trade-observer.ts` — pure DOM reader with `readTransferList()` and `isTransferListPage()`, all selectors imported from `selectors.ts` (AUTO-08 compliant)
- Implemented full TRADE_REPORT pipeline: content script reads DOM → sends TRADE_REPORT to service worker → service worker POSTs to `/api/v1/trade-records/direct`
- Added `reportedOutcomesItem` deduplication storage to prevent double-reporting on page re-render
- Extended `ExtensionMessage` union with `TRADE_REPORT` and `TRADE_REPORT_RESULT` types and updated all exhaustive switches
- 46 total extension tests pass (11 new trade-observer tests + 3 background + 2 content tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create trade-observer module + extend messages and storage** - `a18751f` (feat)
2. **Task 2: Wire service worker handler + content script observer + tests** - `75c2c56` (feat)

## Files Created/Modified

- `extension/src/trade-observer.ts` — pure DOM reader module, exports `readTransferList`, `isTransferListPage`, `DetectedItem`
- `extension/tests/trade-observer.test.ts` — 11 unit tests using jsdom DOM fixtures built from real selector constants
- `extension/src/messages.ts` — extended with `TRADE_REPORT` and `TRADE_REPORT_RESULT` message types
- `extension/src/storage.ts` — added `reportedOutcomesItem` for deduplication (composite key `{ea_id}:{outcome}:{price}`)
- `extension/entrypoints/background.ts` — added `case 'TRADE_REPORT'` + `handleTradeReport()` function
- `extension/entrypoints/ea-webapp.content.ts` — added trade observer imports, `scanTransferList()`, `maybeStartTradeObserver()`, wired into locationchange and initial setup
- `extension/tests/background.test.ts` — 3 new tests for `TRADE_REPORT` handler (success, 404 error, network error)
- `extension/tests/content.test.ts` — 2 new tests for `TRADE_REPORT`/`TRADE_REPORT_RESULT` exhaustive switch cases

## Decisions Made

- `readTransferList()` accepts `Document | Element` root parameter (not `document` global) — enables unit testing with jsdom fixtures without patching globals
- Added `isTimeRemaining()` helper to detect EA's time-remaining strings ("55 Minutes", "1 Hour") as active listings — EA FC26 uses time text for active status, not a simple keyword
- Extended content script exhaustive switch with TRADE_REPORT/TRADE_REPORT_RESULT immediately (in Task 1 commit) to keep TypeScript compiling cleanly throughout

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added TRADE_REPORT cases to content script switch during Task 1**
- **Found during:** Task 1 (after extending messages.ts union)
- **Issue:** TypeScript immediately reported error in ea-webapp.content.ts — new TRADE_REPORT/TRADE_REPORT_RESULT types not handled in exhaustive switch, so `assertNever` received a non-never type. This would have blocked Task 1's `npx tsc --noEmit` acceptance criterion.
- **Fix:** Added `case 'TRADE_REPORT': return false` and `case 'TRADE_REPORT_RESULT': return false` to content script handleMessage switch. These cases were already planned for Task 2 — moved them to Task 1 to unblock TS compilation.
- **Files modified:** extension/entrypoints/ea-webapp.content.ts
- **Verification:** `npx tsc --noEmit` exits 0
- **Committed in:** a18751f (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking)
**Impact on plan:** Cases were already planned for Task 2 — applying them in Task 1 is a reordering, not scope creep. No extra logic added.

## Issues Encountered

None — plan executed as specified with one minor ordering adjustment.

## Known Stubs

None — trade observer reads real DOM, sends real messages, backend relay uses real endpoint from Plan 02.

## Next Phase Readiness

- Trade observation pipeline is complete end-to-end
- Phase 07.2 (Portfolio Dashboard) can read trade records written by this pipeline via existing backend API
- Phase 08 (DOM Automation) builds on the same content script patterns and selector imports established here

## Self-Check: PASSED

- [x] extension/src/trade-observer.ts exists and exports readTransferList, isTransferListPage, DetectedItem
- [x] extension/src/trade-observer.ts imports from './selectors' — no hardcoded CSS strings
- [x] extension/src/messages.ts contains TRADE_REPORT and TRADE_REPORT_RESULT
- [x] extension/src/storage.ts contains reportedOutcomesItem and 'local:reportedOutcomes'
- [x] extension/tests/trade-observer.test.ts exists with 11 test cases
- [x] All 46 extension tests pass (npx vitest run exits 0)
- [x] TypeScript compiles cleanly (npx tsc --noEmit exits 0)
- [x] background.ts contains case 'TRADE_REPORT': and handleTradeReport()
- [x] background.ts contains trade-records/direct in fetch URL
- [x] ea-webapp.content.ts contains maybeStartTradeObserver() and scanTransferList()
- [x] ea-webapp.content.ts imports from trade-observer, storage, selectors
- [x] background.test.ts contains TRADE_REPORT in 9 lines (3+ test cases)
- [x] content.test.ts contains TRADE_REPORT in 6 lines (2+ test cases)
- [x] Task commits a18751f and 75c2c56 exist

---
*Phase: 07.1-trade-reporting*
*Completed: 2026-03-27*
