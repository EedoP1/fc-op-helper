---
phase: 07.1-trade-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/src/selectors.ts
autonomous: false
requirements:
  - AUTO-08
must_haves:
  truths:
    - "selectors.ts exists with real EA Web App CSS selectors discovered from live DevTools inspection"
    - "Each selector constant has a descriptive name and targets a specific DOM element on the Transfer List page"
  artifacts:
    - path: "extension/src/selectors.ts"
      provides: "Centralized selector map for EA Web App Transfer List DOM elements"
      exports: ["TRANSFER_LIST_ITEM", "ITEM_STATUS_LABEL", "ITEM_PLAYER_NAME", "ITEM_BIN_PRICE", "TRANSFER_LIST_CONTAINER"]
  key_links:
    - from: "extension/src/selectors.ts"
      to: "extension/src/trade-observer.ts (Plan 03)"
      via: "named imports"
      pattern: "import.*from.*selectors"
---

<objective>
Create the centralized selector map file (AUTO-08) with placeholder constants, then have the user inspect the live EA Web App Transfer List page in DevTools to fill in real selectors.

Purpose: All EA Web App DOM selectors live in one file so Phase 8 automation and this phase's trade observer both import from the same source. DOM selectors are LOW confidence (per STATE.md blocker) and must be discovered by live inspection before any reader code is written.

Output: `extension/src/selectors.ts` with real, verified CSS selectors for Transfer List player items, statuses, names, and prices.
</objective>

<execution_context>
@C:\Users\maftu\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\maftu\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-trade-reporting/07.1-CONTEXT.md
@.planning/phases/07.1-trade-reporting/07.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create selectors.ts scaffold with placeholder constants</name>
  <files>extension/src/selectors.ts</files>
  <read_first>
    - extension/src/storage.ts (existing centralized module pattern to follow)
    - extension/src/messages.ts (existing centralized module pattern to follow)
  </read_first>
  <action>
Create `extension/src/selectors.ts` exporting named constants for all EA Web App Transfer List DOM selectors needed by the trade observer. Use `'[data-placeholder-TBD]'` as the value for each constant. Constants to define:

```typescript
// extension/src/selectors.ts
// AUTO-08: Centralized selector map for EA Web App DOM elements.
// All selectors discovered via live DevTools inspection (D-04).
// Phase 8 extends this file with automation click-target selectors.

/** Container element wrapping the entire transfer list / trade pile view */
export const TRANSFER_LIST_CONTAINER = '[data-placeholder-TBD]';

/** Individual player card/row in the transfer list */
export const TRANSFER_LIST_ITEM = '[data-placeholder-TBD]';

/** Status label on a player card: "Listed", "Sold", "Expired", etc. */
export const ITEM_STATUS_LABEL = '[data-placeholder-TBD]';

/** Player name text element within a card */
export const ITEM_PLAYER_NAME = '[data-placeholder-TBD]';

/** BIN price displayed on the card */
export const ITEM_BIN_PRICE = '[data-placeholder-TBD]';

/** Start price / current bid displayed on the card */
export const ITEM_START_PRICE = '[data-placeholder-TBD]';
```

Follow the same export-constants-from-module pattern used in `storage.ts` and `messages.ts`. Use UPPER_SNAKE_CASE for constant names. Add JSDoc comment on each constant explaining what DOM element it targets.
  </action>
  <verify>
    <automated>cd C:/Users/maftu/Projects/op-seller/extension && npx tsc --noEmit src/selectors.ts</automated>
  </verify>
  <acceptance_criteria>
    - extension/src/selectors.ts exists
    - File contains `export const TRANSFER_LIST_CONTAINER`
    - File contains `export const TRANSFER_LIST_ITEM`
    - File contains `export const ITEM_STATUS_LABEL`
    - File contains `export const ITEM_PLAYER_NAME`
    - File contains `export const ITEM_BIN_PRICE`
    - File contains `export const ITEM_START_PRICE`
    - All values are placeholder strings containing 'TBD' or 'placeholder'
    - No selector strings appear anywhere else in the extension codebase (AUTO-08 centralization)
  </acceptance_criteria>
  <done>selectors.ts exists with 6+ exported constants, all placeholder values, compiles without errors</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: DOM Exploration — Inspect EA Web App Transfer List in DevTools</name>
  <files>extension/src/selectors.ts</files>
  <action>
User opens the EA Web App in Chrome, navigates to the Transfer List page, and uses DevTools (F12 > Elements tab) to discover real CSS selectors for each placeholder constant in `extension/src/selectors.ts`. User edits the file directly to replace placeholder values with discovered selectors. This is a human-only step because the EA Web App requires authentication and live browser inspection.
  </action>
  <what-built>Selector scaffold in extension/src/selectors.ts with placeholder values that need real selectors from the live EA Web App.</what-built>
  <how-to-verify>
1. Open Chrome and navigate to https://www.ea.com/ea-sports-fc/ultimate-team/web-app/
2. Log in to your FUT account
3. Navigate to the Transfer List page (Transfers > Transfer List)
4. Open DevTools (F12) > Elements tab
5. For each selector constant in `extension/src/selectors.ts`, inspect the DOM and find the real CSS selector:
   - **TRANSFER_LIST_CONTAINER**: The parent element wrapping all listed player cards. Look for a container div/section that holds the item rows.
   - **TRANSFER_LIST_ITEM**: A single player card/row element. Right-click a player card > Inspect. Note the tag + class or data attribute.
   - **ITEM_STATUS_LABEL**: The text element showing "Listed", "Sold", "Expired" on each card.
   - **ITEM_PLAYER_NAME**: The text element showing the player name on each card.
   - **ITEM_BIN_PRICE**: The element showing the BIN (Buy It Now) price on each card.
   - **ITEM_START_PRICE**: The element showing the start/bid price.
6. Also check: Is there an ea_id or resource_id exposed as a data attribute on the player card? If yes, add a new constant `ITEM_EA_ID_ATTR` for it.
7. Edit `extension/src/selectors.ts` and replace all placeholder values with the real selectors discovered.
8. Also check the Trade Pile page (items you've won) — do the same selectors apply, or are different selectors needed? Add any additional constants needed.

**Tips:**
- EA uses obfuscated class names like `.ut-list-item` or custom elements like `<ea-ut-list-item>`. Look for stable patterns (tag names, ARIA roles, data-* attributes) over class names when possible.
- If class names look stable (e.g., `.listFUTItem`), those are fine to use.
- Document any findings about player identification (name text, ea_id attribute, resource_id) as a comment in the file.
  </how-to-verify>
  <verify>
    <automated>grep -c "TBD\|placeholder" C:/Users/maftu/Projects/op-seller/extension/src/selectors.ts | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - extension/src/selectors.ts contains no placeholder values (no 'TBD' or 'placeholder' strings)
    - All `export const` values are real CSS selectors (class selectors, tag selectors, or data attribute selectors)
    - File still compiles: `npx tsc --noEmit src/selectors.ts` exits 0
  </acceptance_criteria>
  <done>All placeholder values replaced with real CSS selectors verified against live EA Web App DOM</done>
  <resume-signal>Type "selectors filled" when all placeholder values in selectors.ts have been replaced with real CSS selectors from DevTools inspection, or describe issues encountered.</resume-signal>
</task>

</tasks>

<verification>
- `extension/src/selectors.ts` exists and compiles: `cd extension && npx tsc --noEmit src/selectors.ts`
- No placeholder values remain: `grep -c 'TBD\|placeholder' extension/src/selectors.ts` returns 0
- All constants are exported: `grep -c 'export const' extension/src/selectors.ts` returns 6 or more
</verification>

<success_criteria>
- selectors.ts contains real EA Web App CSS selectors (no placeholders)
- Each selector has been verified against the live EA Web App DOM
- File compiles without TypeScript errors
- AUTO-08 requirement satisfied: all selectors centralized in one file
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-trade-reporting/07.1-01-SUMMARY.md`
</output>
