---
phase: 07.1-trade-reporting
plan: 01
subsystem: extension
tags: [typescript, dom, selectors, chrome-extension]

# Dependency graph
requires:
  - phase: 06-extension-architecture-foundation
    provides: extension module pattern (storage.ts, messages.ts) to follow for new centralized files
provides:
  - "extension/src/selectors.ts — centralized selector map for EA Web App Transfer List DOM elements"
affects:
  - 07.1-trade-reporting (plans 02, 03 — trade observer reads from selectors.ts)
  - 08-dom-automation (Phase 8 extends selectors.ts with click-target selectors)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Centralized selector constants — all EA Web App CSS selectors in one file (AUTO-08)"
    - "UPPER_SNAKE_CASE exported constants with JSDoc comments per module pattern"

key-files:
  created:
    - extension/src/selectors.ts
  modified: []

key-decisions:
  - "Used class-based selectors (.ut-transfer-list-view, .listFUTItem, .name) — EA FC26 uses stable class names"
  - "BIN price at .auction .auctionValue:nth-child(3) .value — third auctionValue is always Buy Now"
  - "Status text at .auction-state .time — shows Expired or time remaining"
  - "No ea_id/resource_id data attribute on player cards — matching uses player name"

patterns-established:
  - "Selector map file: one exported const per DOM element, JSDoc explains what element it targets"

requirements-completed:
  - AUTO-08

# Metrics
duration: 15min
completed: 2026-03-27
---

# Phase 07.1 Plan 01: Centralized Selector Map Summary

**EA Web App Transfer List selector map (selectors.ts) created with 6 exported constants containing real CSS selectors discovered via live browser inspection.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 of 2 complete
- **Files modified:** 1

## Accomplishments

- Created `extension/src/selectors.ts` following the centralized module pattern from `storage.ts` and `messages.ts`
- Defined 6 exported constants: TRANSFER_LIST_CONTAINER, TRANSFER_LIST_ITEM, ITEM_STATUS_LABEL, ITEM_PLAYER_NAME, ITEM_BIN_PRICE, ITEM_START_PRICE
- Discovered real CSS selectors via live browser automation on the FC26 Web App Transfer List page
- All placeholder values replaced with real selectors verified against live DOM

## Discovered Selectors

| Constant | Selector | Target |
|----------|----------|--------|
| TRANSFER_LIST_CONTAINER | `.ut-transfer-list-view` | Parent wrapper for all transfer list sections |
| TRANSFER_LIST_ITEM | `.listFUTItem` | Individual player card/row |
| ITEM_STATUS_LABEL | `.auction-state .time` | Status text (Expired, time remaining) |
| ITEM_PLAYER_NAME | `.name` | Player name text |
| ITEM_BIN_PRICE | `.auction .auctionValue:nth-child(3) .value` | Buy It Now price |
| ITEM_START_PRICE | `.auctionStartPrice .value` | Start/bid price |

## DOM Structure Notes

- Sections: Sold Items, Unsold Items, Available Items, Active Transfers — each under `.ut-section-header-view`
- Items in `ul.itemList > li.listFUTItem`
- Sold items have class `won` on the `.listFUTItem`
- Status shows "Expired" for unsold, time remaining for active (e.g. "55 Minutes")
- No `ea_id` data attribute found — player identification by name only

## Task Commits

1. **Task 1: Create selectors.ts scaffold** - `2884659` (feat)
2. **Task 2: Fill real selectors via browser inspection** - `6d50264` (feat)

## Files Created/Modified

- `extension/src/selectors.ts` — centralized EA Web App DOM selector map, 6 exported constants with real selectors

## Deviations from Plan

None - plan executed as specified. Browser automation used instead of manual DevTools inspection.

## Self-Check: PASSED

- [x] extension/src/selectors.ts exists and compiles (`npx tsc --noEmit` exits 0)
- [x] No placeholder values remain (0 TBD/placeholder matches)
- [x] All 6 constants exported with real CSS selectors
- [x] AUTO-08 centralization requirement satisfied

---
*Phase: 07.1-trade-reporting*
*Completed: 2026-03-27*
