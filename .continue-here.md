# Continue Here — Scanner Data-Loss Remediation

**Paused:** 2026-04-18 ~07:00 UTC
**Branch:** main
**Last commit:** `4c60bb27` fix(scanner): strip tzinfo from parsed createdAt

## What triggered this session

User asked why `promo_dip_buy` backtest produced no signals today/yesterday with 5M budget. Root cause traced through three layers of silent data loss: (1) Cloudflare-challenge timeouts, (2) `prices=None` treated as successful null scan, (3) `current_bin=None` returning silent None. The real impact: the 2026-04-10 Friday promo batch (Maldini + ~30 others) was invisible to the strategy because `created_at` was never being populated for off-market cards.

## What shipped today (all on main)

### Quick task 260418-c65 — scanner silent-data-loss (3 fixes)
- `playwright_client.py`: Cloudflare challenge now polls every 0.5 s up to 30 s instead of one 5 s sleep. `page.goto` 30 s, sync-bridge 45 s.
- `futgg_client.py` + `scanner.py`: `PricesFetchError` raised when defn valid but prices None. Wired into scanner's `@retry(stop_after_attempt=3)`.
- `scanner_discovery.py`: after upserts, re-queue cards whose `next_scan_at > now+1h` but are back in `discovered_ids` — undoes stale 24 h cold-marks.
- 12/12 new tests.

### Quick task 260418-czo — close current_bin silent-None + rescan stuck cards
- `futgg_client.py`: `current_bin` None/0 no longer returns silent None; returns a shell `PlayerMarketData` so `created_at` populates.
- `scanner.py`: `MarketSnapshot` insert guarded on `current_lowest_bin > 0` so shell path doesn't pollute snapshots.
- `tools/rescan_stuck_players.py`: one-off remediation, **ran once**, requeued 118 pre-fix stuck cards.
- 9/9 new tests.

### Quick task 260418-ddp — remove max-price cap
- `SCANNER_MAX_PRICE = 0` (sentinel: no upper bound).
- `discover_players`: dropped silent `max_price = int(budget * 0.10)` auto-fill that would have sabotaged the sentinel.
- 14/14 tests. Expected to roughly double active card count (~1,800 → 3,000-5,000+).

### Hotfix (not a quick task) `4c60bb27`
- `futgg_client.py`: strip tzinfo from parsed `createdAt` before returning. Column is `TIMESTAMP WITHOUT TIME ZONE`; asyncpg was crashing every scan with a createdAt after czo widened the write path. This was the spam in the logs the user showed.

## What the user needs to do next

1. **Restart the scanner.** The 4c60bb27 fix is on disk but the running process is still crashing every ~2 s with `can't subtract offset-naive and offset-aware datetimes`. Until restart, the DB barely improves.
2. **Observe ~15–30 min after restart**, then verify:
   - `SELECT COUNT(*) FROM players WHERE is_active AND created_at IS NULL AND last_scanned_at IS NOT NULL;` — should drop from 118 toward 0.
   - 2026-04-10 Friday batch should now appear: `SELECT COUNT(*) FROM players WHERE created_at >= '2026-04-10' AND EXTRACT(DOW FROM created_at) = 5;`
   - Maldini (ea_id=100901735) should have `created_at=2026-04-10T17:22:33`, non-zero snapshot count.
3. **Re-run the promo_dip_buy backtest tomorrow** (2026-04-19 UTC) once 2026-04-17 Friday batch has 24 h of hourly snapshots.

## Known open concerns (NOT fixed today)

1. **Discovery-layer problem for next Friday (2026-04-24).** Top-rated promos (TOTY, top TOTS at >500 k release) will still lag into the DB until their price drops below the max. With ddp's cap removal this is now less severe but still possible for >2 M-range cards. User dismissed this pending a "new-releases" endpoint investigation.
2. **118 cards still had `created_at=NULL` as of last check** — after scanner restart they should clear. If any remain stuck, re-run `python -m tools.rescan_stuck_players --yes`.
3. **Pre-existing baseline test failures** (documented in `.planning/quick/260418-c65-.../deferred-items.md`): 14 failures in `test_optimizer.py`, `test_portfolio_swap*`, `test_scanner.py::test_scan_player_writes_score`, `test_cli.py` — drift from the `op_sales/total_sales/op_ratio` column repurposing in quick-260417-sp2. Not related to anything we did today.

## User's uncommitted work on main

- `src/algo/engine.py`, `src/algo/strategies/base.py`, `tests/algo/test_engine.py`, `tests/algo/test_integration.py` had local edits at session start — **committed while I was working** in `d93b1b6f wip(algo): v17 listing-count filter`, `65a0c438 v12_bigsize`, `3fbc082c v19 champion`, `60ea1b96 v20 dip-depth tiered`.
- `backtest_results.json` currently shows `M` in `git status` — user's local backtest artifacts, not touched.

## Planning artifacts

- `.planning/quick/260418-c65-fix-scanner-data-loss-bugs-playwright-ch/` — PLAN, SUMMARY, deferred-items
- `.planning/quick/260418-czo-close-current-bin-silent-none-hole-remed/` — PLAN, SUMMARY
- `.planning/quick/260418-ddp-remove-scanner-max-price-cap-discover-al/` — PLAN, SUMMARY
- `tools/rescan_stuck_players.py` — one-off remediation (already ran)

## Resume

To resume: `/gsd:resume-work`. The scanner restart is the pending manual action. After restart + ~30 min, check DB health per section "What the user needs to do next" above. If Maldini still has no snapshots, the investigation should continue from there.
